"""
Conector AWS S3 — Operaciones de Almacenamiento de Archivos
==============================================================

Permite subir, descargar, listar y gestionar objetos en
buckets de Amazon S3 usando boto3 (preferido) o la API REST
de S3 con firmas AWS Signature V4.
"""

from __future__ import annotations

import hashlib
import hmac
import json
import os
from datetime import UTC, datetime
from typing import Any
from urllib.parse import quote

from src.core.logging import setup_logging
from src.sdk.base import BaseConnector
from src.sdk.http_client import HttpClient, HTTPClientError
from src.sdk.schema import ActionDefinition, AuthRequirement, ConnectorSchema

logger = setup_logging(__name__)

# Intentar importar boto3 para el path preferido
try:
    import boto3
    from botocore.config import Config as BotoConfig

    _BOTO3_AVAILABLE = True
except ImportError:
    _BOTO3_AVAILABLE = False


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_xǁAwsS3Connectorǁ__init____mutmut: MutantDict = {}  # type: ignore
mutants_xǁAwsS3Connectorǁconnect__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAwsS3Connectorǁexecute__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAwsS3Connectorǁvalidate__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAwsS3Connectorǁdisconnect__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAwsS3Connectorǁ_upload_file__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAwsS3Connectorǁ_download_file__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAwsS3Connectorǁ_list_objects__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAwsS3Connectorǁ_delete_object__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAwsS3Connectorǁ_get_object_url__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAwsS3Connectorǁ_copy_object__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAwsS3Connectorǁ_signed_request__mutmut: MutantDict = {}  # type: ignore
mutants_xǁAwsS3Connectorǁ_get_signing_key__mutmut: MutantDict = {}  # type: ignore


class AwsS3Connector(BaseConnector):
    """Conector para AWS S3: operaciones CRUD en buckets y objetos.

    Usa boto3 si esta disponible; de lo contrario, recurre a la
    API REST de S3 con firmas AWS Signature V4 via HttpClient.
    """

    name = "aws_s3"
    version = "1.0.0"
    description = "Sube, descarga y gestiona archivos en buckets de Amazon S3"
    category = "cloud_storage"
    icon = "hard-drive"
    author = "Zenic-Flijo"

    @_mutmut_mutated(mutants_xǁAwsS3Connectorǁ__init____mutmut)
    def __init__(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://s3.amazonaws.com"
        self._http: HttpClient | None = None
        self._boto_client: Any = None
        # AWS credentials — extraidos del auth_provider o params
        self._access_key_id: str = ""
        self._secret_access_key: str = ""
        self._region: str = "us-east-1"
        self._session_token: str = ""

    def xǁAwsS3Connectorǁ__init____mutmut_orig(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://s3.amazonaws.com"
        self._http: HttpClient | None = None
        self._boto_client: Any = None
        # AWS credentials — extraidos del auth_provider o params
        self._access_key_id: str = ""
        self._secret_access_key: str = ""
        self._region: str = "us-east-1"
        self._session_token: str = ""

    def xǁAwsS3Connectorǁ__init____mutmut_1(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = None
        self._http: HttpClient | None = None
        self._boto_client: Any = None
        # AWS credentials — extraidos del auth_provider o params
        self._access_key_id: str = ""
        self._secret_access_key: str = ""
        self._region: str = "us-east-1"
        self._session_token: str = ""

    def xǁAwsS3Connectorǁ__init____mutmut_2(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "XXhttps://s3.amazonaws.comXX"
        self._http: HttpClient | None = None
        self._boto_client: Any = None
        # AWS credentials — extraidos del auth_provider o params
        self._access_key_id: str = ""
        self._secret_access_key: str = ""
        self._region: str = "us-east-1"
        self._session_token: str = ""

    def xǁAwsS3Connectorǁ__init____mutmut_3(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "HTTPS://S3.AMAZONAWS.COM"
        self._http: HttpClient | None = None
        self._boto_client: Any = None
        # AWS credentials — extraidos del auth_provider o params
        self._access_key_id: str = ""
        self._secret_access_key: str = ""
        self._region: str = "us-east-1"
        self._session_token: str = ""

    def xǁAwsS3Connectorǁ__init____mutmut_4(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://s3.amazonaws.com"
        self._http: HttpClient | None = ""
        self._boto_client: Any = None
        # AWS credentials — extraidos del auth_provider o params
        self._access_key_id: str = ""
        self._secret_access_key: str = ""
        self._region: str = "us-east-1"
        self._session_token: str = ""

    def xǁAwsS3Connectorǁ__init____mutmut_5(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://s3.amazonaws.com"
        self._http: HttpClient | None = None
        self._boto_client: Any = ""
        # AWS credentials — extraidos del auth_provider o params
        self._access_key_id: str = ""
        self._secret_access_key: str = ""
        self._region: str = "us-east-1"
        self._session_token: str = ""

    def xǁAwsS3Connectorǁ__init____mutmut_6(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://s3.amazonaws.com"
        self._http: HttpClient | None = None
        self._boto_client: Any = None
        # AWS credentials — extraidos del auth_provider o params
        self._access_key_id: str = None
        self._secret_access_key: str = ""
        self._region: str = "us-east-1"
        self._session_token: str = ""

    def xǁAwsS3Connectorǁ__init____mutmut_7(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://s3.amazonaws.com"
        self._http: HttpClient | None = None
        self._boto_client: Any = None
        # AWS credentials — extraidos del auth_provider o params
        self._access_key_id: str = "XXXX"
        self._secret_access_key: str = ""
        self._region: str = "us-east-1"
        self._session_token: str = ""

    def xǁAwsS3Connectorǁ__init____mutmut_8(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://s3.amazonaws.com"
        self._http: HttpClient | None = None
        self._boto_client: Any = None
        # AWS credentials — extraidos del auth_provider o params
        self._access_key_id: str = ""
        self._secret_access_key: str = None
        self._region: str = "us-east-1"
        self._session_token: str = ""

    def xǁAwsS3Connectorǁ__init____mutmut_9(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://s3.amazonaws.com"
        self._http: HttpClient | None = None
        self._boto_client: Any = None
        # AWS credentials — extraidos del auth_provider o params
        self._access_key_id: str = ""
        self._secret_access_key: str = "XXXX"
        self._region: str = "us-east-1"
        self._session_token: str = ""

    def xǁAwsS3Connectorǁ__init____mutmut_10(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://s3.amazonaws.com"
        self._http: HttpClient | None = None
        self._boto_client: Any = None
        # AWS credentials — extraidos del auth_provider o params
        self._access_key_id: str = ""
        self._secret_access_key: str = ""
        self._region: str = None
        self._session_token: str = ""

    def xǁAwsS3Connectorǁ__init____mutmut_11(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://s3.amazonaws.com"
        self._http: HttpClient | None = None
        self._boto_client: Any = None
        # AWS credentials — extraidos del auth_provider o params
        self._access_key_id: str = ""
        self._secret_access_key: str = ""
        self._region: str = "XXus-east-1XX"
        self._session_token: str = ""

    def xǁAwsS3Connectorǁ__init____mutmut_12(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://s3.amazonaws.com"
        self._http: HttpClient | None = None
        self._boto_client: Any = None
        # AWS credentials — extraidos del auth_provider o params
        self._access_key_id: str = ""
        self._secret_access_key: str = ""
        self._region: str = "US-EAST-1"
        self._session_token: str = ""

    def xǁAwsS3Connectorǁ__init____mutmut_13(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://s3.amazonaws.com"
        self._http: HttpClient | None = None
        self._boto_client: Any = None
        # AWS credentials — extraidos del auth_provider o params
        self._access_key_id: str = ""
        self._secret_access_key: str = ""
        self._region: str = "us-east-1"
        self._session_token: str = None

    def xǁAwsS3Connectorǁ__init____mutmut_14(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://s3.amazonaws.com"
        self._http: HttpClient | None = None
        self._boto_client: Any = None
        # AWS credentials — extraidos del auth_provider o params
        self._access_key_id: str = ""
        self._secret_access_key: str = ""
        self._region: str = "us-east-1"
        self._session_token: str = "XXXX"

    # ── Conexion ────────────────────────────────────────────────

    @_mutmut_mutated(mutants_xǁAwsS3Connectorǁconnect__mutmut)
    def connect(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_orig(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_1(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider and not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_2(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_3(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_4(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error(None)
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_5(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("XXAwsS3Connector: credenciales AWS no configuradasXX")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_6(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("awss3connector: credenciales aws no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_7(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AWSS3CONNECTOR: CREDENCIALES AWS NO CONFIGURADAS")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_8(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return True

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_9(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = None
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_10(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(None, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_11(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, None) else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_12(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr("get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_13(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, ) else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_14(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "XXget_credentialsXX") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_15(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "GET_CREDENTIALS") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_16(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = None
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_17(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get(None, os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_18(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", None)
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_19(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get(os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_20(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", )
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_21(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("XXaccess_key_idXX", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_22(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("ACCESS_KEY_ID", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_23(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get(None, ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_24(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", None))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_25(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get(""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_26(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_27(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("XXAWS_ACCESS_KEY_IDXX", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_28(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("aws_access_key_id", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_29(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", "XXXX"))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_30(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = None
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_31(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get(None, os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_32(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", None)
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_33(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get(os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_34(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", )
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_35(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("XXsecret_access_keyXX", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_36(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("SECRET_ACCESS_KEY", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_37(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get(None, ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_38(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", None))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_39(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get(""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_40(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_41(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("XXAWS_SECRET_ACCESS_KEYXX", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_42(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("aws_secret_access_key", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_43(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", "XXXX"))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_44(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = None
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_45(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get(None, os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_46(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", None)
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_47(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get(os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_48(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", )
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_49(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("XXregionXX", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_50(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("REGION", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_51(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get(None, "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_52(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", None))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_53(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_54(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", ))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_55(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("XXAWS_DEFAULT_REGIONXX", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_56(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("aws_default_region", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_57(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "XXus-east-1XX"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_58(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "US-EAST-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_59(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = None

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_60(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get(None, os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_61(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", None)

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_62(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get(os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_63(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", )

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_64(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("XXsession_tokenXX", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_65(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("SESSION_TOKEN", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_66(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get(None, ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_67(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", None))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_68(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get(""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_69(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_70(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("XXAWS_SESSION_TOKENXX", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_71(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("aws_session_token", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_72(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", "XXXX"))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_73(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id and not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_74(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_75(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_76(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error(None)
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_77(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("XXAwsS3Connector: access_key_id o secret_access_key no proporcionadosXX")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_78(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("awss3connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_79(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AWSS3CONNECTOR: ACCESS_KEY_ID O SECRET_ACCESS_KEY NO PROPORCIONADOS")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_80(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return True

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_81(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = None
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_82(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    None,
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_83(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=None,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_84(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=None,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_85(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_86(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=None,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_87(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=None,
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_88(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_89(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_90(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_91(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_92(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_93(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_94(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "XXs3XX",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_95(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "S3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_96(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token and None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_97(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries=None),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_98(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"XXmax_attemptsXX": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_99(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"MAX_ATTEMPTS": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_100(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 4, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_101(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "XXmodeXX": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_102(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "MODE": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_103(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "XXstandardXX"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_104(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "STANDARD"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_105(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = None
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_106(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = False
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_107(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation(None, f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_108(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", None)
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_109(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation(f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_110(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", )
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_111(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("XXconnectXX", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_112(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("CONNECT", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_113(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return False
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_114(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(None)

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_115(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = None
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_116(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=None,
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_117(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=None,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_118(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=None,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_119(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_120(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_121(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_122(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=31,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_123(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = None
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_124(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request(None, "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_125(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", None, {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_126(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", None)
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_127(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_128(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_129(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", )
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_130(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("XXGETXX", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_131(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("get", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_132(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "XX/XX", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_133(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code not in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_134(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (201, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_135(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 404, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_136(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 405):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_137(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = None
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_138(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = False
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_139(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation(None, f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_140(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", None)
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_141(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation(f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_142(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", )
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_143(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("XXconnectXX", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_144(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("CONNECT", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_145(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return False
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_146(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(None)
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_147(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return True
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_148(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(None)
            return False

    # ── Conexion ────────────────────────────────────────────────

    def xǁAwsS3Connectorǁconnect__mutmut_149(self) -> bool:
        """Establece conexion con AWS S3 usando las credenciales configuradas."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("AwsS3Connector: credenciales AWS no configuradas")
            return False

        # Extraer credenciales del auth_provider
        creds = self._auth_provider.get_credentials() if hasattr(self._auth_provider, "get_credentials") else {}
        self._access_key_id = creds.get("access_key_id", os.environ.get("AWS_ACCESS_KEY_ID", ""))
        self._secret_access_key = creds.get("secret_access_key", os.environ.get("AWS_SECRET_ACCESS_KEY", ""))
        self._region = creds.get("region", os.environ.get("AWS_DEFAULT_REGION", "us-east-1"))
        self._session_token = creds.get("session_token", os.environ.get("AWS_SESSION_TOKEN", ""))

        if not self._access_key_id or not self._secret_access_key:
            logger.error("AwsS3Connector: access_key_id o secret_access_key no proporcionados")
            return False

        # Inicializar boto3 si esta disponible
        if _BOTO3_AVAILABLE:
            try:
                self._boto_client = boto3.client(
                    "s3",
                    aws_access_key_id=self._access_key_id,
                    aws_secret_access_key=self._secret_access_key,
                    aws_session_token=self._session_token or None,
                    region_name=self._region,
                    config=BotoConfig(retries={"max_attempts": 3, "mode": "standard"}),
                )
                # Verificar que la conexion funciona listando buckets (operacion ligera)
                self._boto_client.list_buckets()
                self._connected = True
                self._log_operation("connect", f"boto3 OK, region={self._region}")
                return True
            except Exception as exc:
                logger.warning(f"AwsS3Connector: boto3 fallo ({exc}), usando REST API")

        # Fallback: REST API con HttpClient
        self._http = HttpClient(
            base_url=f"https://s3.{self._region}.amazonaws.com",
            connector_name=self.name,
            timeout=30,
        )
        # Verificar conexion con una peticion HEAD al servicio
        try:
            resp = self._signed_request("GET", "/", {})
            if resp.status_code in (200, 403, 404):
                # 403/404 son aceptables — significan que el endpoint responde
                self._connected = True
                self._log_operation("connect", f"REST API OK, region={self._region}")
                return True
            logger.error(f"AwsS3Connector: REST API respondio {resp.status_code}")
            return False
        except HTTPClientError as exc:
            logger.error(f"AwsS3Connector: REST API fallo: {exc}")
            return True

    @_mutmut_mutated(mutants_xǁAwsS3Connectorǁexecute__mutmut)
    def execute(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector AWS S3."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "list_objects": self._list_objects,
            "delete_object": self._delete_object,
            "get_object_url": self._get_object_url,
            "copy_object": self._copy_object,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAwsS3Connectorǁexecute__mutmut_orig(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector AWS S3."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "list_objects": self._list_objects,
            "delete_object": self._delete_object,
            "get_object_url": self._get_object_url,
            "copy_object": self._copy_object,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAwsS3Connectorǁexecute__mutmut_1(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector AWS S3."""
        action_map: dict[str, Any] = None
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAwsS3Connectorǁexecute__mutmut_2(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector AWS S3."""
        action_map: dict[str, Any] = {
            "XXupload_fileXX": self._upload_file,
            "download_file": self._download_file,
            "list_objects": self._list_objects,
            "delete_object": self._delete_object,
            "get_object_url": self._get_object_url,
            "copy_object": self._copy_object,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAwsS3Connectorǁexecute__mutmut_3(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector AWS S3."""
        action_map: dict[str, Any] = {
            "UPLOAD_FILE": self._upload_file,
            "download_file": self._download_file,
            "list_objects": self._list_objects,
            "delete_object": self._delete_object,
            "get_object_url": self._get_object_url,
            "copy_object": self._copy_object,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAwsS3Connectorǁexecute__mutmut_4(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector AWS S3."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "XXdownload_fileXX": self._download_file,
            "list_objects": self._list_objects,
            "delete_object": self._delete_object,
            "get_object_url": self._get_object_url,
            "copy_object": self._copy_object,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAwsS3Connectorǁexecute__mutmut_5(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector AWS S3."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "DOWNLOAD_FILE": self._download_file,
            "list_objects": self._list_objects,
            "delete_object": self._delete_object,
            "get_object_url": self._get_object_url,
            "copy_object": self._copy_object,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAwsS3Connectorǁexecute__mutmut_6(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector AWS S3."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "XXlist_objectsXX": self._list_objects,
            "delete_object": self._delete_object,
            "get_object_url": self._get_object_url,
            "copy_object": self._copy_object,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAwsS3Connectorǁexecute__mutmut_7(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector AWS S3."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "LIST_OBJECTS": self._list_objects,
            "delete_object": self._delete_object,
            "get_object_url": self._get_object_url,
            "copy_object": self._copy_object,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAwsS3Connectorǁexecute__mutmut_8(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector AWS S3."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "list_objects": self._list_objects,
            "XXdelete_objectXX": self._delete_object,
            "get_object_url": self._get_object_url,
            "copy_object": self._copy_object,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAwsS3Connectorǁexecute__mutmut_9(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector AWS S3."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "list_objects": self._list_objects,
            "DELETE_OBJECT": self._delete_object,
            "get_object_url": self._get_object_url,
            "copy_object": self._copy_object,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAwsS3Connectorǁexecute__mutmut_10(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector AWS S3."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "list_objects": self._list_objects,
            "delete_object": self._delete_object,
            "XXget_object_urlXX": self._get_object_url,
            "copy_object": self._copy_object,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAwsS3Connectorǁexecute__mutmut_11(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector AWS S3."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "list_objects": self._list_objects,
            "delete_object": self._delete_object,
            "GET_OBJECT_URL": self._get_object_url,
            "copy_object": self._copy_object,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAwsS3Connectorǁexecute__mutmut_12(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector AWS S3."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "list_objects": self._list_objects,
            "delete_object": self._delete_object,
            "get_object_url": self._get_object_url,
            "XXcopy_objectXX": self._copy_object,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAwsS3Connectorǁexecute__mutmut_13(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector AWS S3."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "list_objects": self._list_objects,
            "delete_object": self._delete_object,
            "get_object_url": self._get_object_url,
            "COPY_OBJECT": self._copy_object,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAwsS3Connectorǁexecute__mutmut_14(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector AWS S3."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "list_objects": self._list_objects,
            "delete_object": self._delete_object,
            "get_object_url": self._get_object_url,
            "copy_object": self._copy_object,
        }
        handler = None
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAwsS3Connectorǁexecute__mutmut_15(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector AWS S3."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "list_objects": self._list_objects,
            "delete_object": self._delete_object,
            "get_object_url": self._get_object_url,
            "copy_object": self._copy_object,
        }
        handler = action_map.get(None)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAwsS3Connectorǁexecute__mutmut_16(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector AWS S3."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "list_objects": self._list_objects,
            "delete_object": self._delete_object,
            "get_object_url": self._get_object_url,
            "copy_object": self._copy_object,
        }
        handler = action_map.get(action)
        if handler is not None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAwsS3Connectorǁexecute__mutmut_17(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector AWS S3."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "list_objects": self._list_objects,
            "delete_object": self._delete_object,
            "get_object_url": self._get_object_url,
            "copy_object": self._copy_object,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"XXerrorXX": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAwsS3Connectorǁexecute__mutmut_18(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector AWS S3."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "list_objects": self._list_objects,
            "delete_object": self._delete_object,
            "get_object_url": self._get_object_url,
            "copy_object": self._copy_object,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"ERROR": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁAwsS3Connectorǁexecute__mutmut_19(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector AWS S3."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "list_objects": self._list_objects,
            "delete_object": self._delete_object,
            "get_object_url": self._get_object_url,
            "copy_object": self._copy_object,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "XXavailableXX": list(action_map.keys())}
        return handler(params)

    def xǁAwsS3Connectorǁexecute__mutmut_20(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector AWS S3."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "list_objects": self._list_objects,
            "delete_object": self._delete_object,
            "get_object_url": self._get_object_url,
            "copy_object": self._copy_object,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "AVAILABLE": list(action_map.keys())}
        return handler(params)

    def xǁAwsS3Connectorǁexecute__mutmut_21(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector AWS S3."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "list_objects": self._list_objects,
            "delete_object": self._delete_object,
            "get_object_url": self._get_object_url,
            "copy_object": self._copy_object,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(None)}
        return handler(params)

    def xǁAwsS3Connectorǁexecute__mutmut_22(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector AWS S3."""
        action_map: dict[str, Any] = {
            "upload_file": self._upload_file,
            "download_file": self._download_file,
            "list_objects": self._list_objects,
            "delete_object": self._delete_object,
            "get_object_url": self._get_object_url,
            "copy_object": self._copy_object,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(None)

    @_mutmut_mutated(mutants_xǁAwsS3Connectorǁvalidate__mutmut)
    def validate(self) -> bool:
        """Valida que las credenciales AWS esten configuradas."""
        if not self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁAwsS3Connectorǁvalidate__mutmut_orig(self) -> bool:
        """Valida que las credenciales AWS esten configuradas."""
        if not self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁAwsS3Connectorǁvalidate__mutmut_1(self) -> bool:
        """Valida que las credenciales AWS esten configuradas."""
        if self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁAwsS3Connectorǁvalidate__mutmut_2(self) -> bool:
        """Valida que las credenciales AWS esten configuradas."""
        if not self._auth_provider:
            return True
        return self._auth_provider.validate()

    @_mutmut_mutated(mutants_xǁAwsS3Connectorǁdisconnect__mutmut)
    def disconnect(self) -> bool:
        """Cierra la conexion con AWS S3."""
        self._connected = False
        self._boto_client = None
        self._http = None
        self._log_operation("disconnect")
        return True

    def xǁAwsS3Connectorǁdisconnect__mutmut_orig(self) -> bool:
        """Cierra la conexion con AWS S3."""
        self._connected = False
        self._boto_client = None
        self._http = None
        self._log_operation("disconnect")
        return True

    def xǁAwsS3Connectorǁdisconnect__mutmut_1(self) -> bool:
        """Cierra la conexion con AWS S3."""
        self._connected = None
        self._boto_client = None
        self._http = None
        self._log_operation("disconnect")
        return True

    def xǁAwsS3Connectorǁdisconnect__mutmut_2(self) -> bool:
        """Cierra la conexion con AWS S3."""
        self._connected = True
        self._boto_client = None
        self._http = None
        self._log_operation("disconnect")
        return True

    def xǁAwsS3Connectorǁdisconnect__mutmut_3(self) -> bool:
        """Cierra la conexion con AWS S3."""
        self._connected = False
        self._boto_client = ""
        self._http = None
        self._log_operation("disconnect")
        return True

    def xǁAwsS3Connectorǁdisconnect__mutmut_4(self) -> bool:
        """Cierra la conexion con AWS S3."""
        self._connected = False
        self._boto_client = None
        self._http = ""
        self._log_operation("disconnect")
        return True

    def xǁAwsS3Connectorǁdisconnect__mutmut_5(self) -> bool:
        """Cierra la conexion con AWS S3."""
        self._connected = False
        self._boto_client = None
        self._http = None
        self._log_operation(None)
        return True

    def xǁAwsS3Connectorǁdisconnect__mutmut_6(self) -> bool:
        """Cierra la conexion con AWS S3."""
        self._connected = False
        self._boto_client = None
        self._http = None
        self._log_operation("XXdisconnectXX")
        return True

    def xǁAwsS3Connectorǁdisconnect__mutmut_7(self) -> bool:
        """Cierra la conexion con AWS S3."""
        self._connected = False
        self._boto_client = None
        self._http = None
        self._log_operation("DISCONNECT")
        return True

    def xǁAwsS3Connectorǁdisconnect__mutmut_8(self) -> bool:
        """Cierra la conexion con AWS S3."""
        self._connected = False
        self._boto_client = None
        self._http = None
        self._log_operation("disconnect")
        return False

    # ── Acciones ───────────────────────────────────────────────

    @_mutmut_mutated(mutants_xǁAwsS3Connectorǁ_upload_file__mutmut)
    def _upload_file(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = None
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get(None, "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", None)
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", )
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("XXbucketXX", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("BUCKET", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "XXXX")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = None
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get(None, "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", None)
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", )
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("XXkeyXX", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("KEY", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "XXXX")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = None
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get(None)
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("XXcontentXX")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("CONTENT")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = None

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get(None)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("XXfile_pathXX")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("FILE_PATH")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket and not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"XXsuccessXX": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"SUCCESS": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": True, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "XXerrorXX": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "ERROR": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "XXParametros requeridos: bucket, keyXX"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "PARAMETROS REQUERIDOS: BUCKET, KEY"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None or not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is not None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"XXsuccessXX": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"SUCCESS": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": True, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "XXerrorXX": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "ERROR": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "XXDebe proporcionar 'content' o 'file_path'XX"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "DEBE PROPORCIONAR 'CONTENT' O 'FILE_PATH'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None or file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is not None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(None, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, None) as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open("rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, ) as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "XXrbXX") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "RB") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = None
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"XXsuccessXX": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"SUCCESS": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": True, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "XXerrorXX": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "ERROR": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = None

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode(None)

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("XXutf-8XX")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("UTF-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = None
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get(None, "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", None)
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", )
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("XXcontent_typeXX", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("CONTENT_TYPE", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "XXapplication/octet-streamXX")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "APPLICATION/OCTET-STREAM")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation(None, f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", None)

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation(f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", )

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("XXupload_fileXX", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("UPLOAD_FILE", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = None
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"XXContentTypeXX": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"contenttype": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"CONTENTTYPE": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=None, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=None, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=None, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "XXsuccessXX": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "SUCCESS": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": False,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "XXbucketXX": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "BUCKET": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "XXkeyXX": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "KEY": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "XXsizeXX": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "SIZE": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"XXsuccessXX": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"SUCCESS": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": True, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "XXerrorXX": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "ERROR": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = None
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                None,
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                None,
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                None,
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=None,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers=None,
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "XXPUTXX",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "put",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"XXContent-TypeXX": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"content-type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"CONTENT-TYPE": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = None
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get(None, "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", None)
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("XXETagXX", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("etag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETAG", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "XXXX")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "XXsuccessXX": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "SUCCESS": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": False,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_134(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "XXbucketXX": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_135(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "BUCKET": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_136(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "XXkeyXX": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_137(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "KEY": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_138(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "XXetagXX": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_139(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "ETAG": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_140(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "XXsizeXX": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_141(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "SIZE": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_142(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"XXsuccessXX": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_143(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"SUCCESS": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_144(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": True, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_145(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "XXerrorXX": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_146(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "ERROR": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_147(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"XXsuccessXX": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_148(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"SUCCESS": False, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_149(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": True, "error": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_150(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "XXerrorXX": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_151(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "ERROR": str(exc)}

    # ── Acciones ───────────────────────────────────────────────

    def xǁAwsS3Connectorǁ_upload_file__mutmut_152(self, params: dict[str, Any]) -> dict[str, Any]:
        """Sube un archivo a un bucket de S3.

        Args:
            params: Debe contener 'bucket', 'key' y 'content' (o 'file_path')
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        content = params.get("content")
        file_path = params.get("file_path")

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}
        if content is None and not file_path:
            return {"success": False, "error": "Debe proporcionar 'content' o 'file_path'"}

        # Leer contenido
        if content is None and file_path:
            try:
                with open(file_path, "rb") as f:
                    content = f.read()
            except OSError as exc:
                return {"success": False, "error": f"Error leyendo archivo: {exc}"}

        if isinstance(content, str):
            content = content.encode("utf-8")

        content_type = params.get("content_type", "application/octet-stream")
        self._log_operation("upload_file", f"bucket={bucket}, key={key}, size={len(content)}")

        # boto3 path
        if self._boto_client is not None:
            try:
                extra_args: dict[str, Any] = {"ContentType": content_type}
                self._boto_client.put_object(
                    Bucket=bucket, Key=key, Body=content, **extra_args
                )
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "size": len(content),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 put_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{bucket}/{key}",
                {},
                body=content,
                headers={"Content-Type": content_type},
            )
            if resp.ok:
                etag = resp.headers.get("ETag", "")
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "etag": etag,
                    "size": len(content),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(None)}

    @_mutmut_mutated(mutants_xǁAwsS3Connectorǁ_download_file__mutmut)
    def _download_file(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = None
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get(None, "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", None)
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", )
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("XXbucketXX", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("BUCKET", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "XXXX")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = None
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get(None, "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", None)
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", )
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("XXkeyXX", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("KEY", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "XXXX")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket and not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"XXsuccessXX": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"SUCCESS": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": True, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "XXerrorXX": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "ERROR": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "XXParametros requeridos: bucket, keyXX"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "PARAMETROS REQUERIDOS: BUCKET, KEY"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation(None, f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", None)

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation(f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", )

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("XXdownload_fileXX", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("DOWNLOAD_FILE", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = None
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=None, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=None)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, )
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = None
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["XXBodyXX"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["BODY"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "XXsuccessXX": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "SUCCESS": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": False,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "XXbucketXX": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "BUCKET": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "XXkeyXX": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "KEY": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "XXcontentXX": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "CONTENT": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "XXsize_bytesXX": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "SIZE_BYTES": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "XXcontent_typeXX": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "CONTENT_TYPE": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get(None, "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", None),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", ),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("XXContentTypeXX", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("contenttype", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("CONTENTTYPE", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "XXapplication/octet-streamXX"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "APPLICATION/OCTET-STREAM"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"XXsuccessXX": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"SUCCESS": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": True, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "XXerrorXX": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "ERROR": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = None
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(None, f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", None, {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", None)
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", )
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("XXGETXX", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("get", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "XXsuccessXX": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "SUCCESS": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": False,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "XXbucketXX": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "BUCKET": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "XXkeyXX": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "KEY": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "XXcontentXX": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "CONTENT": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "XXsize_bytesXX": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "SIZE_BYTES": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 1,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "XXcontent_typeXX": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "CONTENT_TYPE": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get(None, "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", None),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", ),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("XXContent-TypeXX", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("content-type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("CONTENT-TYPE", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "XXapplication/octet-streamXX"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "APPLICATION/OCTET-STREAM"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"XXsuccessXX": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"SUCCESS": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": True, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "XXerrorXX": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "ERROR": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"XXsuccessXX": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"SUCCESS": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": True, "error": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "XXerrorXX": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "ERROR": str(exc)}

    def xǁAwsS3Connectorǁ_download_file__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Descarga un archivo de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("download_file", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                response = self._boto_client.get_object(Bucket=bucket, Key=key)
                body = response["Body"].read()
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": body,
                    "size_bytes": len(body),
                    "content_type": response.get("ContentType", "application/octet-stream"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 get_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("GET", f"/{bucket}/{key}", {})
            if resp.ok:
                return {
                    "success": True,
                    "bucket": bucket,
                    "key": key,
                    "content": resp.raw,
                    "size_bytes": len(resp.raw) if resp.raw else 0,
                    "content_type": resp.headers.get("Content-Type", "application/octet-stream"),
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(None)}

    @_mutmut_mutated(mutants_xǁAwsS3Connectorǁ_list_objects__mutmut)
    def _list_objects(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = None
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get(None, "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", None)
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", )
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("XXbucketXX", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("BUCKET", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "XXXX")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = None
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get(None, "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", None)
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", )
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("XXprefixXX", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("PREFIX", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "XXXX")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = None
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get(None, 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", None)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get(1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", )
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("XXlimitXX", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("LIMIT", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1001)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = None

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get(None, "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", None)

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", )

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("XXmarkerXX", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("MARKER", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "XXXX")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"XXsuccessXX": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"SUCCESS": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": True, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "XXerrorXX": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "ERROR": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "XXParametro requerido: bucketXX"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "PARAMETRO REQUERIDO: BUCKET"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation(None, f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", None)

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation(f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", )

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("XXlist_objectsXX", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("LIST_OBJECTS", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = None
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"XXBucketXX": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"BUCKET": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "XXMaxKeysXX": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "maxkeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MAXKEYS": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = None
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["XXPrefixXX"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["PREFIX"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = None

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["XXMarkerXX"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["MARKER"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = None
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = None
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get(None, []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", None):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get([]):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", ):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("XXContentsXX", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("CONTENTS", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append(None)
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "XXkeyXX": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "KEY": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["XXKeyXX"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["KEY"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "XXsizeXX": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "SIZE": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["XXSizeXX"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["SIZE"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "XXlast_modifiedXX": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "LAST_MODIFIED": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["XXLastModifiedXX"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["lastmodified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LASTMODIFIED"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(None, "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], None) else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr("isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], ) else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["XXLastModifiedXX"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["lastmodified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LASTMODIFIED"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "XXisoformatXX") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "ISOFORMAT") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(None),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["XXLastModifiedXX"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["lastmodified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LASTMODIFIED"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "XXetagXX": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "ETAG": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get(None, ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", None),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get(""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("XXETagXX", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("etag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETAG", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", "XXXX"),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "XXstorage_classXX": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "STORAGE_CLASS": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get(None, "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", None),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", ),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("XXStorageClassXX", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("storageclass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("STORAGECLASS", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "XXSTANDARDXX"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "standard"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "XXsuccessXX": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "SUCCESS": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": False,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "XXbucketXX": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "BUCKET": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "XXobjectsXX": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "OBJECTS": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "XXtotalXX": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "TOTAL": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get(None, len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", None),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_134(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get(len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_135(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", ),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_136(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("XXKeyCountXX", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_137(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("keycount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_138(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KEYCOUNT", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_139(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "XXis_truncatedXX": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_140(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "IS_TRUNCATED": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_141(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get(None, False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_142(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", None),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_143(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get(False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_144(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", ),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_145(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("XXIsTruncatedXX", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_146(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("istruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_147(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("ISTRUNCATED", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_148(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", True),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_149(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "XXnext_continuation_tokenXX": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_150(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "NEXT_CONTINUATION_TOKEN": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_151(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get(None, ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_152(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", None),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_153(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get(""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_154(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_155(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("XXNextContinuationTokenXX", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_156(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("nextcontinuationtoken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_157(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NEXTCONTINUATIONTOKEN", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_158(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", "XXXX"),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_159(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"XXsuccessXX": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_160(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"SUCCESS": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_161(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": True, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_162(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "XXerrorXX": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_163(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "ERROR": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_164(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = None
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_165(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"XXlist-typeXX": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_166(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"LIST-TYPE": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_167(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "XX2XX", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_168(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "XXmax-keysXX": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_169(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "MAX-KEYS": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_170(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(None)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_171(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = None
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_172(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["XXprefixXX"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_173(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["PREFIX"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_174(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = None

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_175(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["XXstart-afterXX"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_176(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["START-AFTER"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_177(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = None
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_178(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request(None, f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_179(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", None, query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_180(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", None)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_181(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request(f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_182(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_183(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", )
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_184(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("XXGETXX", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_185(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("get", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_186(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = None
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_187(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = None
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_188(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = None
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_189(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get(None, []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_190(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", None) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_191(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get([]) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_192(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", ) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_193(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("XXContentsXX", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_194(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_195(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("CONTENTS", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_196(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append(None)
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_197(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "XXkeyXX": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_198(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "KEY": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_199(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get(None, ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_200(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", None),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_201(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get(""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_202(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_203(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("XXKeyXX", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_204(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_205(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("KEY", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_206(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", "XXXX"),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_207(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "XXsizeXX": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_208(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "SIZE": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_209(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get(None, 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_210(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", None),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_211(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get(0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_212(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", ),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_213(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("XXSizeXX", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_214(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_215(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("SIZE", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_216(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 1),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_217(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "XXlast_modifiedXX": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_218(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "LAST_MODIFIED": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_219(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get(None, ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_220(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", None),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_221(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get(""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_222(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_223(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("XXLastModifiedXX", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_224(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("lastmodified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_225(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LASTMODIFIED", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_226(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", "XXXX"),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_227(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "XXetagXX": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_228(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "ETAG": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_229(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get(None, ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_230(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", None),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_231(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get(""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_232(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_233(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("XXETagXX", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_234(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("etag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_235(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETAG", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_236(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", "XXXX"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_237(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "XXsuccessXX": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_238(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "SUCCESS": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_239(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": False,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_240(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "XXbucketXX": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_241(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "BUCKET": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_242(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "XXobjectsXX": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_243(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "OBJECTS": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_244(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "XXtotalXX": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_245(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "TOTAL": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_246(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "XXis_truncatedXX": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_247(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "IS_TRUNCATED": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_248(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get(None, "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_249(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", None) == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_250(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_251(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", ) == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_252(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("XXIsTruncatedXX", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_253(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("istruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_254(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("ISTRUNCATED", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_255(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "XXfalseXX") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_256(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "FALSE") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_257(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") != "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_258(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "XXtrueXX" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_259(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "TRUE" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_260(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else True,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_261(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"XXsuccessXX": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_262(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"SUCCESS": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_263(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": True, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_264(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "XXerrorXX": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_265(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "ERROR": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_266(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"XXsuccessXX": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_267(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"SUCCESS": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_268(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": True, "error": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_269(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "XXerrorXX": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_270(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "ERROR": str(exc)}

    def xǁAwsS3Connectorǁ_list_objects__mutmut_271(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista objetos en un bucket de S3.

        Args:
            params: Debe contener 'bucket' y opcionalmente 'prefix', 'limit', 'marker'
        """
        bucket = params.get("bucket", "")
        prefix = params.get("prefix", "")
        limit = params.get("limit", 1000)
        marker = params.get("marker", "")

        if not bucket:
            return {"success": False, "error": "Parametro requerido: bucket"}

        self._log_operation("list_objects", f"bucket={bucket}, prefix={prefix}")

        # boto3 path
        if self._boto_client is not None:
            try:
                kwargs: dict[str, Any] = {"Bucket": bucket, "MaxKeys": limit}
                if prefix:
                    kwargs["Prefix"] = prefix
                if marker:
                    kwargs["Marker"] = marker

                response = self._boto_client.list_objects_v2(**kwargs)
                objects = []
                for obj in response.get("Contents", []):
                    objects.append({
                        "key": obj["Key"],
                        "size": obj["Size"],
                        "last_modified": obj["LastModified"].isoformat() if hasattr(obj["LastModified"], "isoformat") else str(obj["LastModified"]),
                        "etag": obj.get("ETag", ""),
                        "storage_class": obj.get("StorageClass", "STANDARD"),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": response.get("KeyCount", len(objects)),
                    "is_truncated": response.get("IsTruncated", False),
                    "next_continuation_token": response.get("NextContinuationToken", ""),
                }
            except Exception as exc:
                return {"success": False, "error": f"boto3 list_objects_v2 fallo: {exc}"}

        # REST API path
        try:
            query_params: dict[str, Any] = {"list-type": "2", "max-keys": str(limit)}
            if prefix:
                query_params["prefix"] = prefix
            if marker:
                query_params["start-after"] = marker

            resp = self._signed_request("GET", f"/{bucket}", query_params)
            if resp.ok:
                body = resp.json() if resp.body else {}
                # Parsear la respuesta XML/JSON de S3
                objects = []
                # S3 list v2 devuelve XML por defecto; parsear basico
                content_list = body.get("Contents", []) if isinstance(body, dict) else []
                for obj in content_list:
                    objects.append({
                        "key": obj.get("Key", ""),
                        "size": obj.get("Size", 0),
                        "last_modified": obj.get("LastModified", ""),
                        "etag": obj.get("ETag", ""),
                    })
                return {
                    "success": True,
                    "bucket": bucket,
                    "objects": objects,
                    "total": len(objects),
                    "is_truncated": body.get("IsTruncated", "false") == "true" if isinstance(body, dict) else False,
                }
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(None)}

    @_mutmut_mutated(mutants_xǁAwsS3Connectorǁ_delete_object__mutmut)
    def _delete_object(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = None
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get(None, "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", None)
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", )
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("XXbucketXX", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("BUCKET", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "XXXX")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = None
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get(None, "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", None)
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", )
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("XXkeyXX", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("KEY", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "XXXX")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket and not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"XXsuccessXX": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"SUCCESS": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": True, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "XXerrorXX": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "ERROR": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "XXParametros requeridos: bucket, keyXX"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "PARAMETROS REQUERIDOS: BUCKET, KEY"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation(None, f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", None)

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation(f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", )

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("XXdelete_objectXX", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("DELETE_OBJECT", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=None, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=None)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, )
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"XXsuccessXX": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"SUCCESS": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": False, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "XXbucketXX": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "BUCKET": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "XXkeyXX": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "KEY": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "XXdeletedXX": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "DELETED": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": False}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"XXsuccessXX": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"SUCCESS": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": True, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "XXerrorXX": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "ERROR": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = None
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(None, f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", None, {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", None)
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", )
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("XXDELETEXX", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("delete", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok and resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code != 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 205:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"XXsuccessXX": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"SUCCESS": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": False, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "XXbucketXX": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "BUCKET": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "XXkeyXX": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "KEY": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "XXdeletedXX": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "DELETED": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": False}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"XXsuccessXX": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"SUCCESS": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": True, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "XXerrorXX": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "ERROR": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"XXsuccessXX": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"SUCCESS": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": True, "error": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "XXerrorXX": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "ERROR": str(exc)}

    def xǁAwsS3Connectorǁ_delete_object__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Elimina un objeto de un bucket de S3.

        Args:
            params: Debe contener 'bucket' y 'key'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("delete_object", f"bucket={bucket}, key={key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                self._boto_client.delete_object(Bucket=bucket, Key=key)
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            except Exception as exc:
                return {"success": False, "error": f"boto3 delete_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request("DELETE", f"/{bucket}/{key}", {})
            if resp.ok or resp.status_code == 204:
                return {"success": True, "bucket": bucket, "key": key, "deleted": True}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(None)}

    @_mutmut_mutated(mutants_xǁAwsS3Connectorǁ_get_object_url__mutmut)
    def _get_object_url(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = None
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get(None, "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", None)
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", )
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("XXbucketXX", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("BUCKET", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "XXXX")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = None
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get(None, "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", None)
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", )
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("XXkeyXX", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("KEY", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "XXXX")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = None

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get(None, 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", None)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get(3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", )

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("XXexpires_inXX", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("EXPIRES_IN", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3601)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket and not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"XXsuccessXX": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"SUCCESS": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": True, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "XXerrorXX": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "ERROR": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "XXParametros requeridos: bucket, keyXX"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "PARAMETROS REQUERIDOS: BUCKET, KEY"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation(None, f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", None)

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation(f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", )

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("XXget_object_urlXX", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("GET_OBJECT_URL", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = None
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    None,
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params=None,
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=None,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "XXget_objectXX",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "GET_OBJECT",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"XXBucketXX": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"BUCKET": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "XXKeyXX": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "KEY": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"XXsuccessXX": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"SUCCESS": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": False, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "XXurlXX": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "URL": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "XXexpires_inXX": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "EXPIRES_IN": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"XXsuccessXX": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"SUCCESS": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": True, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "XXerrorXX": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "ERROR": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = None
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"XXsuccessXX": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"SUCCESS": True, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": False, "url": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "XXurlXX": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "URL": url, "expires_in": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "XXexpires_inXX": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "EXPIRES_IN": expires_in, "note": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "XXnoteXX": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "NOTE": "URL sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "XXURL sin firmar (boto3 no disponible)XX"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "url sin firmar (boto3 no disponible)"}

    def xǁAwsS3Connectorǁ_get_object_url__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Genera una URL firmada para acceder a un objeto de S3.

        Args:
            params: Debe contener 'bucket', 'key' y opcionalmente 'expires_in'
        """
        bucket = params.get("bucket", "")
        key = params.get("key", "")
        expires_in = params.get("expires_in", 3600)

        if not bucket or not key:
            return {"success": False, "error": "Parametros requeridos: bucket, key"}

        self._log_operation("get_object_url", f"bucket={bucket}, key={key}")

        # boto3 path — genera URL firmada real
        if self._boto_client is not None:
            try:
                url = self._boto_client.generate_presigned_url(
                    "get_object",
                    Params={"Bucket": bucket, "Key": key},
                    ExpiresIn=expires_in,
                )
                return {"success": True, "url": url, "expires_in": expires_in}
            except Exception as exc:
                return {"success": False, "error": f"boto3 generate_presigned_url fallo: {exc}"}

        # REST API path — URL directa sin firmar (para buckets publicos)
        url = f"https://{bucket}.s3.{self._region}.amazonaws.com/{key}"
        return {"success": True, "url": url, "expires_in": expires_in, "note": "URL SIN FIRMAR (BOTO3 NO DISPONIBLE)"}

    @_mutmut_mutated(mutants_xǁAwsS3Connectorǁ_copy_object__mutmut)
    def _copy_object(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = None
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get(None, "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", None)
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", )
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("XXsource_bucketXX", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("SOURCE_BUCKET", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "XXXX")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = None
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get(None, "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", None)
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", )
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("XXsource_keyXX", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("SOURCE_KEY", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "XXXX")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = None
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get(None, "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", None)
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", )
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("XXdest_bucketXX", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("DEST_BUCKET", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "XXXX")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = None

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get(None, "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", None)

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", )

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("XXdest_keyXX", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("DEST_KEY", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "XXXX")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket and not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key and not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket and not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"XXsuccessXX": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"SUCCESS": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": True, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "XXerrorXX": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "ERROR": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "XXParametros requeridos: source_bucket, source_key, dest_bucket, dest_keyXX"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "PARAMETROS REQUERIDOS: SOURCE_BUCKET, SOURCE_KEY, DEST_BUCKET, DEST_KEY"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation(None, f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", None)

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation(f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", )

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("XXcopy_objectXX", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("COPY_OBJECT", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = None
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"XXBucketXX": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"BUCKET": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "XXKeyXX": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "KEY": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=None, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=None, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=None
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"XXsuccessXX": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"SUCCESS": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": False, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "XXdest_bucketXX": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "DEST_BUCKET": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "XXdest_keyXX": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "DEST_KEY": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"XXsuccessXX": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"SUCCESS": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": True, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "XXerrorXX": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "ERROR": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = None
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                None,
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                None,
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                None,
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers=None,
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "XXPUTXX",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "put",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"XXx-amz-copy-sourceXX": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"X-AMZ-COPY-SOURCE": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"XXsuccessXX": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"SUCCESS": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": False, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "XXdest_bucketXX": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "DEST_BUCKET": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "XXdest_keyXX": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "DEST_KEY": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"XXsuccessXX": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"SUCCESS": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": True, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "XXerrorXX": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "ERROR": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"XXsuccessXX": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"SUCCESS": False, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": True, "error": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "XXerrorXX": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "ERROR": str(exc)}

    def xǁAwsS3Connectorǁ_copy_object__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Copia un objeto entre buckets o dentro del mismo bucket.

        Args:
            params: Debe contener 'source_bucket', 'source_key', 'dest_bucket', 'dest_key'
        """
        source_bucket = params.get("source_bucket", "")
        source_key = params.get("source_key", "")
        dest_bucket = params.get("dest_bucket", "")
        dest_key = params.get("dest_key", "")

        if not source_bucket or not source_key or not dest_bucket or not dest_key:
            return {"success": False, "error": "Parametros requeridos: source_bucket, source_key, dest_bucket, dest_key"}

        self._log_operation("copy_object", f"{source_bucket}/{source_key} -> {dest_bucket}/{dest_key}")

        # boto3 path
        if self._boto_client is not None:
            try:
                copy_source = {"Bucket": source_bucket, "Key": source_key}
                self._boto_client.copy_object(
                    CopySource=copy_source, Bucket=dest_bucket, Key=dest_key
                )
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            except Exception as exc:
                return {"success": False, "error": f"boto3 copy_object fallo: {exc}"}

        # REST API path
        try:
            resp = self._signed_request(
                "PUT",
                f"/{dest_bucket}/{dest_key}",
                {},
                headers={"x-amz-copy-source": f"/{source_bucket}/{source_key}"},
            )
            if resp.ok:
                return {"success": True, "dest_bucket": dest_bucket, "dest_key": dest_key}
            return {"success": False, "error": f"HTTP {resp.status_code}: {resp.body}"}
        except HTTPClientError as exc:
            return {"success": False, "error": str(None)}

    # ── AWS Signature V4 ───────────────────────────────────────

    @_mutmut_mutated(mutants_xǁAwsS3Connectorǁ_signed_request__mutmut)
    def _signed_request(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_orig(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_1(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_2(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message=None)

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_3(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="XXHttpClient no inicializado. Llame connect() primero.XX")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_4(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="httpclient no inicializado. llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_5(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HTTPCLIENT NO INICIALIZADO. LLAME CONNECT() PRIMERO.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_6(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = None
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_7(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(None)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_8(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = None
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_9(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime(None)
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_10(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("XX%Y%m%dT%H%M%SZXX")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_11(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%y%m%dt%h%m%sz")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_12(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%M%DT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_13(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = None

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_14(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime(None)

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_15(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("XX%Y%m%dXX")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_16(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_17(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%M%D")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_18(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = None

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_19(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            None
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_20(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "XX&XX".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_21(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(None, safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_22(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe=None)}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_23(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_24(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), )}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_25(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(None), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_26(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='XXXX')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_27(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(None, safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_28(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe=None)}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_29(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_30(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), )}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_31(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(None), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_32(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='XXXX')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_33(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(None)
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_34(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is not None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_35(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = None
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_36(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b"XXXX"
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_37(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = None

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_38(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(None).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_39(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = None
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_40(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = None
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_41(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "XXhostXX": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_42(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "HOST": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_43(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "XXx-amz-dateXX": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_44(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "X-AMZ-DATE": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_45(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "XXx-amz-content-sha256XX": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_46(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "X-AMZ-CONTENT-SHA256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_47(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = None

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_48(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["XXx-amz-security-tokenXX"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_49(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["X-AMZ-SECURITY-TOKEN"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_50(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = None

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_51(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.upper()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_52(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = None
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_53(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = "XXXX"
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_54(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = None
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_55(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(None)
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_56(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers = f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_57(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers -= f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_58(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = None

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_59(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(None)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_60(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = "XX;XX".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_61(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = None

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_62(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join(None)

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_63(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "XX\nXX".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_64(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = None
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_65(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = None

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_66(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join(None)

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_67(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "XX\nXX".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_68(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "XXAWS4-HMAC-SHA256XX",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_69(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "aws4-hmac-sha256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_70(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(None).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_71(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode(None)).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_72(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("XXutf-8XX")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_73(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("UTF-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_74(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = None

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_75(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(None)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_76(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = None

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_77(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(None, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_78(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, None, hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_79(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), None).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_80(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_81(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_82(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), ).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_83(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode(None), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_84(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("XXutf-8XX"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_85(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("UTF-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_86(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = None

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_87(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = None
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_88(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "XXAuthorizationXX": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_89(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_90(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "AUTHORIZATION": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_91(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "XXx-amz-dateXX": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_92(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "X-AMZ-DATE": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_93(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "XXx-amz-content-sha256XX": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_94(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "X-AMZ-CONTENT-SHA256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_95(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = None
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_96(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["XXx-amz-security-tokenXX"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_97(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["X-AMZ-SECURITY-TOKEN"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_98(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(None)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_99(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = None
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_100(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url = f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_101(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url -= f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_102(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = None

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_103(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "XXmethodXX": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_104(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "METHOD": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_105(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "XXurlXX": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_106(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "URL": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_107(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "XXheadersXX": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_108(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "HEADERS": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_109(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "XXdataXX": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_110(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "DATA": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_111(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "XXtimeoutXX": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_112(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "TIMEOUT": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_113(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 31,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_114(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "XXverifyXX": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_115(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "VERIFY": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_116(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": False,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_117(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = None
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_118(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = None
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_119(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = None

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_120(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=None,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_121(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=None,
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_122(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=None,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_123(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=None,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_124(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=None,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_125(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=None,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_126(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=None,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_127(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_128(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_129(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_130(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_131(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_132(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_133(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_134(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(None),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_135(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(None, "total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_136(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, None) else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_137(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr("total_seconds") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_138(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, ) else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_139(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "XXtotal_secondsXX") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_140(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "TOTAL_SECONDS") else 0,
            url=url,
            method=method,
        )

    # ── AWS Signature V4 ───────────────────────────────────────

    def xǁAwsS3Connectorǁ_signed_request__mutmut_141(
        self,
        method: str,
        path: str,
        query_params: dict[str, Any],
        body: bytes | None = None,
        headers: dict[str, str] | None = None,
    ) -> Any:
        """Ejecuta una peticion HTTP firmada con AWS Signature V4.

        Args:
            method: Metodo HTTP (GET, PUT, DELETE, etc.)
            path: Ruta del recurso (e.g. /bucket/key)
            query_params: Parametros de query string
            body: Cuerpo de la peticion (bytes)
            headers: Headers adicionales

        Returns:
            HTTPResponse del HttpClient
        """
        if not self._http:
            raise HTTPClientError(message="HttpClient no inicializado. Llame connect() primero.")

        now = datetime.now(UTC)
        amz_date = now.strftime("%Y%m%dT%H%M%SZ")
        date_stamp = now.strftime("%Y%m%d")

        # Construir canonical query string
        canonical_querystring = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}"
            for k, v in sorted(query_params.items())
        )

        # Preparar payload
        if body is None:
            body = b""
        payload_hash = hashlib.sha256(body).hexdigest()

        # Headers para firmar
        host = f"s3.{self._region}.amazonaws.com"
        signed_headers_dict: dict[str, str] = {
            "host": host,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            signed_headers_dict["x-amz-security-token"] = self._session_token

        # Headers adicionales del caller
        if headers:
            for k, v in headers.items():
                signed_headers_dict[k.lower()] = v

        # Canonical headers y signed headers
        canonical_headers = ""
        signed_headers_list = sorted(signed_headers_dict.keys())
        for h in signed_headers_list:
            canonical_headers += f"{h}:{signed_headers_dict[h]}\n"
        signed_headers = ";".join(signed_headers_list)

        # Canonical request
        canonical_request = "\n".join([
            method,
            path,
            canonical_querystring,
            canonical_headers,
            signed_headers,
            payload_hash,
        ])

        # String to sign
        credential_scope = f"{date_stamp}/{self._region}/s3/aws4_request"
        string_to_sign = "\n".join([
            "AWS4-HMAC-SHA256",
            amz_date,
            credential_scope,
            hashlib.sha256(canonical_request.encode("utf-8")).hexdigest(),
        ])

        # Signing key
        signing_key = self._get_signing_key(date_stamp)

        # Signature
        signature = hmac.new(signing_key, string_to_sign.encode("utf-8"), hashlib.sha256).hexdigest()

        # Authorization header
        authorization = (
            f"AWS4-HMAC-SHA256 "
            f"Credential={self._access_key_id}/{credential_scope}, "
            f"SignedHeaders={signed_headers}, "
            f"Signature={signature}"
        )

        # Construir headers finales
        final_headers: dict[str, str] = {
            "Authorization": authorization,
            "x-amz-date": amz_date,
            "x-amz-content-sha256": payload_hash,
        }
        if self._session_token:
            final_headers["x-amz-security-token"] = self._session_token
        if headers:
            final_headers.update(headers)

        # Ejecutar la peticion usando HttpClient internamente
        # Necesitamos usar requests directamente porque HttpClient no soporta body bytes
        import requests as req_lib

        url = f"https://{host}{path}"
        if canonical_querystring:
            url += f"?{canonical_querystring}"

        request_kwargs: dict[str, Any] = {
            "method": method,
            "url": url,
            "headers": final_headers,
            "data": body if body else None,
            "timeout": 30,
            "verify": True,
        }

        resp = req_lib.request(**request_kwargs)
        from src.sdk.http_client import HTTPResponse

        resp_body: Any
        try:
            resp_body = resp.json()
        except (json.JSONDecodeError, ValueError):
            resp_body = resp.text

        return HTTPResponse(
            status_code=resp.status_code,
            headers=dict(resp.headers),
            body=resp_body,
            raw=resp.content,
            elapsed=resp.elapsed.total_seconds() if hasattr(resp.elapsed, "total_seconds") else 1,
            url=url,
            method=method,
        )

    @_mutmut_mutated(mutants_xǁAwsS3Connectorǁ_get_signing_key__mutmut)
    def _get_signing_key(self, date_stamp: str) -> bytes:
        """Deriva la clave de firma AWS Signature V4.

        Args:
            date_stamp: Fecha en formato YYYYMMDD

        Returns:
            Clave de firma en bytes
        """
        k_date = hmac.new(
            f"AWS4{self._secret_access_key}".encode(),
            date_stamp.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        k_region = hmac.new(k_date, self._region.encode("utf-8"), hashlib.sha256).digest()
        k_service = hmac.new(k_region, b"s3", hashlib.sha256).digest()
        k_signing = hmac.new(k_service, b"aws4_request", hashlib.sha256).digest()
        return k_signing

    def xǁAwsS3Connectorǁ_get_signing_key__mutmut_orig(self, date_stamp: str) -> bytes:
        """Deriva la clave de firma AWS Signature V4.

        Args:
            date_stamp: Fecha en formato YYYYMMDD

        Returns:
            Clave de firma en bytes
        """
        k_date = hmac.new(
            f"AWS4{self._secret_access_key}".encode(),
            date_stamp.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        k_region = hmac.new(k_date, self._region.encode("utf-8"), hashlib.sha256).digest()
        k_service = hmac.new(k_region, b"s3", hashlib.sha256).digest()
        k_signing = hmac.new(k_service, b"aws4_request", hashlib.sha256).digest()
        return k_signing

    def xǁAwsS3Connectorǁ_get_signing_key__mutmut_1(self, date_stamp: str) -> bytes:
        """Deriva la clave de firma AWS Signature V4.

        Args:
            date_stamp: Fecha en formato YYYYMMDD

        Returns:
            Clave de firma en bytes
        """
        k_date = None
        k_region = hmac.new(k_date, self._region.encode("utf-8"), hashlib.sha256).digest()
        k_service = hmac.new(k_region, b"s3", hashlib.sha256).digest()
        k_signing = hmac.new(k_service, b"aws4_request", hashlib.sha256).digest()
        return k_signing

    def xǁAwsS3Connectorǁ_get_signing_key__mutmut_2(self, date_stamp: str) -> bytes:
        """Deriva la clave de firma AWS Signature V4.

        Args:
            date_stamp: Fecha en formato YYYYMMDD

        Returns:
            Clave de firma en bytes
        """
        k_date = hmac.new(
            None,
            date_stamp.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        k_region = hmac.new(k_date, self._region.encode("utf-8"), hashlib.sha256).digest()
        k_service = hmac.new(k_region, b"s3", hashlib.sha256).digest()
        k_signing = hmac.new(k_service, b"aws4_request", hashlib.sha256).digest()
        return k_signing

    def xǁAwsS3Connectorǁ_get_signing_key__mutmut_3(self, date_stamp: str) -> bytes:
        """Deriva la clave de firma AWS Signature V4.

        Args:
            date_stamp: Fecha en formato YYYYMMDD

        Returns:
            Clave de firma en bytes
        """
        k_date = hmac.new(
            f"AWS4{self._secret_access_key}".encode(),
            None,
            hashlib.sha256,
        ).digest()
        k_region = hmac.new(k_date, self._region.encode("utf-8"), hashlib.sha256).digest()
        k_service = hmac.new(k_region, b"s3", hashlib.sha256).digest()
        k_signing = hmac.new(k_service, b"aws4_request", hashlib.sha256).digest()
        return k_signing

    def xǁAwsS3Connectorǁ_get_signing_key__mutmut_4(self, date_stamp: str) -> bytes:
        """Deriva la clave de firma AWS Signature V4.

        Args:
            date_stamp: Fecha en formato YYYYMMDD

        Returns:
            Clave de firma en bytes
        """
        k_date = hmac.new(
            f"AWS4{self._secret_access_key}".encode(),
            date_stamp.encode("utf-8"),
            None,
        ).digest()
        k_region = hmac.new(k_date, self._region.encode("utf-8"), hashlib.sha256).digest()
        k_service = hmac.new(k_region, b"s3", hashlib.sha256).digest()
        k_signing = hmac.new(k_service, b"aws4_request", hashlib.sha256).digest()
        return k_signing

    def xǁAwsS3Connectorǁ_get_signing_key__mutmut_5(self, date_stamp: str) -> bytes:
        """Deriva la clave de firma AWS Signature V4.

        Args:
            date_stamp: Fecha en formato YYYYMMDD

        Returns:
            Clave de firma en bytes
        """
        k_date = hmac.new(
            date_stamp.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        k_region = hmac.new(k_date, self._region.encode("utf-8"), hashlib.sha256).digest()
        k_service = hmac.new(k_region, b"s3", hashlib.sha256).digest()
        k_signing = hmac.new(k_service, b"aws4_request", hashlib.sha256).digest()
        return k_signing

    def xǁAwsS3Connectorǁ_get_signing_key__mutmut_6(self, date_stamp: str) -> bytes:
        """Deriva la clave de firma AWS Signature V4.

        Args:
            date_stamp: Fecha en formato YYYYMMDD

        Returns:
            Clave de firma en bytes
        """
        k_date = hmac.new(
            f"AWS4{self._secret_access_key}".encode(),
            hashlib.sha256,
        ).digest()
        k_region = hmac.new(k_date, self._region.encode("utf-8"), hashlib.sha256).digest()
        k_service = hmac.new(k_region, b"s3", hashlib.sha256).digest()
        k_signing = hmac.new(k_service, b"aws4_request", hashlib.sha256).digest()
        return k_signing

    def xǁAwsS3Connectorǁ_get_signing_key__mutmut_7(self, date_stamp: str) -> bytes:
        """Deriva la clave de firma AWS Signature V4.

        Args:
            date_stamp: Fecha en formato YYYYMMDD

        Returns:
            Clave de firma en bytes
        """
        k_date = hmac.new(
            f"AWS4{self._secret_access_key}".encode(),
            date_stamp.encode("utf-8"),
            ).digest()
        k_region = hmac.new(k_date, self._region.encode("utf-8"), hashlib.sha256).digest()
        k_service = hmac.new(k_region, b"s3", hashlib.sha256).digest()
        k_signing = hmac.new(k_service, b"aws4_request", hashlib.sha256).digest()
        return k_signing

    def xǁAwsS3Connectorǁ_get_signing_key__mutmut_8(self, date_stamp: str) -> bytes:
        """Deriva la clave de firma AWS Signature V4.

        Args:
            date_stamp: Fecha en formato YYYYMMDD

        Returns:
            Clave de firma en bytes
        """
        k_date = hmac.new(
            f"AWS4{self._secret_access_key}".encode(),
            date_stamp.encode(None),
            hashlib.sha256,
        ).digest()
        k_region = hmac.new(k_date, self._region.encode("utf-8"), hashlib.sha256).digest()
        k_service = hmac.new(k_region, b"s3", hashlib.sha256).digest()
        k_signing = hmac.new(k_service, b"aws4_request", hashlib.sha256).digest()
        return k_signing

    def xǁAwsS3Connectorǁ_get_signing_key__mutmut_9(self, date_stamp: str) -> bytes:
        """Deriva la clave de firma AWS Signature V4.

        Args:
            date_stamp: Fecha en formato YYYYMMDD

        Returns:
            Clave de firma en bytes
        """
        k_date = hmac.new(
            f"AWS4{self._secret_access_key}".encode(),
            date_stamp.encode("XXutf-8XX"),
            hashlib.sha256,
        ).digest()
        k_region = hmac.new(k_date, self._region.encode("utf-8"), hashlib.sha256).digest()
        k_service = hmac.new(k_region, b"s3", hashlib.sha256).digest()
        k_signing = hmac.new(k_service, b"aws4_request", hashlib.sha256).digest()
        return k_signing

    def xǁAwsS3Connectorǁ_get_signing_key__mutmut_10(self, date_stamp: str) -> bytes:
        """Deriva la clave de firma AWS Signature V4.

        Args:
            date_stamp: Fecha en formato YYYYMMDD

        Returns:
            Clave de firma en bytes
        """
        k_date = hmac.new(
            f"AWS4{self._secret_access_key}".encode(),
            date_stamp.encode("UTF-8"),
            hashlib.sha256,
        ).digest()
        k_region = hmac.new(k_date, self._region.encode("utf-8"), hashlib.sha256).digest()
        k_service = hmac.new(k_region, b"s3", hashlib.sha256).digest()
        k_signing = hmac.new(k_service, b"aws4_request", hashlib.sha256).digest()
        return k_signing

    def xǁAwsS3Connectorǁ_get_signing_key__mutmut_11(self, date_stamp: str) -> bytes:
        """Deriva la clave de firma AWS Signature V4.

        Args:
            date_stamp: Fecha en formato YYYYMMDD

        Returns:
            Clave de firma en bytes
        """
        k_date = hmac.new(
            f"AWS4{self._secret_access_key}".encode(),
            date_stamp.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        k_region = None
        k_service = hmac.new(k_region, b"s3", hashlib.sha256).digest()
        k_signing = hmac.new(k_service, b"aws4_request", hashlib.sha256).digest()
        return k_signing

    def xǁAwsS3Connectorǁ_get_signing_key__mutmut_12(self, date_stamp: str) -> bytes:
        """Deriva la clave de firma AWS Signature V4.

        Args:
            date_stamp: Fecha en formato YYYYMMDD

        Returns:
            Clave de firma en bytes
        """
        k_date = hmac.new(
            f"AWS4{self._secret_access_key}".encode(),
            date_stamp.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        k_region = hmac.new(None, self._region.encode("utf-8"), hashlib.sha256).digest()
        k_service = hmac.new(k_region, b"s3", hashlib.sha256).digest()
        k_signing = hmac.new(k_service, b"aws4_request", hashlib.sha256).digest()
        return k_signing

    def xǁAwsS3Connectorǁ_get_signing_key__mutmut_13(self, date_stamp: str) -> bytes:
        """Deriva la clave de firma AWS Signature V4.

        Args:
            date_stamp: Fecha en formato YYYYMMDD

        Returns:
            Clave de firma en bytes
        """
        k_date = hmac.new(
            f"AWS4{self._secret_access_key}".encode(),
            date_stamp.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        k_region = hmac.new(k_date, None, hashlib.sha256).digest()
        k_service = hmac.new(k_region, b"s3", hashlib.sha256).digest()
        k_signing = hmac.new(k_service, b"aws4_request", hashlib.sha256).digest()
        return k_signing

    def xǁAwsS3Connectorǁ_get_signing_key__mutmut_14(self, date_stamp: str) -> bytes:
        """Deriva la clave de firma AWS Signature V4.

        Args:
            date_stamp: Fecha en formato YYYYMMDD

        Returns:
            Clave de firma en bytes
        """
        k_date = hmac.new(
            f"AWS4{self._secret_access_key}".encode(),
            date_stamp.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        k_region = hmac.new(k_date, self._region.encode("utf-8"), None).digest()
        k_service = hmac.new(k_region, b"s3", hashlib.sha256).digest()
        k_signing = hmac.new(k_service, b"aws4_request", hashlib.sha256).digest()
        return k_signing

    def xǁAwsS3Connectorǁ_get_signing_key__mutmut_15(self, date_stamp: str) -> bytes:
        """Deriva la clave de firma AWS Signature V4.

        Args:
            date_stamp: Fecha en formato YYYYMMDD

        Returns:
            Clave de firma en bytes
        """
        k_date = hmac.new(
            f"AWS4{self._secret_access_key}".encode(),
            date_stamp.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        k_region = hmac.new(self._region.encode("utf-8"), hashlib.sha256).digest()
        k_service = hmac.new(k_region, b"s3", hashlib.sha256).digest()
        k_signing = hmac.new(k_service, b"aws4_request", hashlib.sha256).digest()
        return k_signing

    def xǁAwsS3Connectorǁ_get_signing_key__mutmut_16(self, date_stamp: str) -> bytes:
        """Deriva la clave de firma AWS Signature V4.

        Args:
            date_stamp: Fecha en formato YYYYMMDD

        Returns:
            Clave de firma en bytes
        """
        k_date = hmac.new(
            f"AWS4{self._secret_access_key}".encode(),
            date_stamp.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        k_region = hmac.new(k_date, hashlib.sha256).digest()
        k_service = hmac.new(k_region, b"s3", hashlib.sha256).digest()
        k_signing = hmac.new(k_service, b"aws4_request", hashlib.sha256).digest()
        return k_signing

    def xǁAwsS3Connectorǁ_get_signing_key__mutmut_17(self, date_stamp: str) -> bytes:
        """Deriva la clave de firma AWS Signature V4.

        Args:
            date_stamp: Fecha en formato YYYYMMDD

        Returns:
            Clave de firma en bytes
        """
        k_date = hmac.new(
            f"AWS4{self._secret_access_key}".encode(),
            date_stamp.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        k_region = hmac.new(k_date, self._region.encode("utf-8"), ).digest()
        k_service = hmac.new(k_region, b"s3", hashlib.sha256).digest()
        k_signing = hmac.new(k_service, b"aws4_request", hashlib.sha256).digest()
        return k_signing

    def xǁAwsS3Connectorǁ_get_signing_key__mutmut_18(self, date_stamp: str) -> bytes:
        """Deriva la clave de firma AWS Signature V4.

        Args:
            date_stamp: Fecha en formato YYYYMMDD

        Returns:
            Clave de firma en bytes
        """
        k_date = hmac.new(
            f"AWS4{self._secret_access_key}".encode(),
            date_stamp.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        k_region = hmac.new(k_date, self._region.encode(None), hashlib.sha256).digest()
        k_service = hmac.new(k_region, b"s3", hashlib.sha256).digest()
        k_signing = hmac.new(k_service, b"aws4_request", hashlib.sha256).digest()
        return k_signing

    def xǁAwsS3Connectorǁ_get_signing_key__mutmut_19(self, date_stamp: str) -> bytes:
        """Deriva la clave de firma AWS Signature V4.

        Args:
            date_stamp: Fecha en formato YYYYMMDD

        Returns:
            Clave de firma en bytes
        """
        k_date = hmac.new(
            f"AWS4{self._secret_access_key}".encode(),
            date_stamp.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        k_region = hmac.new(k_date, self._region.encode("XXutf-8XX"), hashlib.sha256).digest()
        k_service = hmac.new(k_region, b"s3", hashlib.sha256).digest()
        k_signing = hmac.new(k_service, b"aws4_request", hashlib.sha256).digest()
        return k_signing

    def xǁAwsS3Connectorǁ_get_signing_key__mutmut_20(self, date_stamp: str) -> bytes:
        """Deriva la clave de firma AWS Signature V4.

        Args:
            date_stamp: Fecha en formato YYYYMMDD

        Returns:
            Clave de firma en bytes
        """
        k_date = hmac.new(
            f"AWS4{self._secret_access_key}".encode(),
            date_stamp.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        k_region = hmac.new(k_date, self._region.encode("UTF-8"), hashlib.sha256).digest()
        k_service = hmac.new(k_region, b"s3", hashlib.sha256).digest()
        k_signing = hmac.new(k_service, b"aws4_request", hashlib.sha256).digest()
        return k_signing

    def xǁAwsS3Connectorǁ_get_signing_key__mutmut_21(self, date_stamp: str) -> bytes:
        """Deriva la clave de firma AWS Signature V4.

        Args:
            date_stamp: Fecha en formato YYYYMMDD

        Returns:
            Clave de firma en bytes
        """
        k_date = hmac.new(
            f"AWS4{self._secret_access_key}".encode(),
            date_stamp.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        k_region = hmac.new(k_date, self._region.encode("utf-8"), hashlib.sha256).digest()
        k_service = None
        k_signing = hmac.new(k_service, b"aws4_request", hashlib.sha256).digest()
        return k_signing

    def xǁAwsS3Connectorǁ_get_signing_key__mutmut_22(self, date_stamp: str) -> bytes:
        """Deriva la clave de firma AWS Signature V4.

        Args:
            date_stamp: Fecha en formato YYYYMMDD

        Returns:
            Clave de firma en bytes
        """
        k_date = hmac.new(
            f"AWS4{self._secret_access_key}".encode(),
            date_stamp.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        k_region = hmac.new(k_date, self._region.encode("utf-8"), hashlib.sha256).digest()
        k_service = hmac.new(None, b"s3", hashlib.sha256).digest()
        k_signing = hmac.new(k_service, b"aws4_request", hashlib.sha256).digest()
        return k_signing

    def xǁAwsS3Connectorǁ_get_signing_key__mutmut_23(self, date_stamp: str) -> bytes:
        """Deriva la clave de firma AWS Signature V4.

        Args:
            date_stamp: Fecha en formato YYYYMMDD

        Returns:
            Clave de firma en bytes
        """
        k_date = hmac.new(
            f"AWS4{self._secret_access_key}".encode(),
            date_stamp.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        k_region = hmac.new(k_date, self._region.encode("utf-8"), hashlib.sha256).digest()
        k_service = hmac.new(k_region, None, hashlib.sha256).digest()
        k_signing = hmac.new(k_service, b"aws4_request", hashlib.sha256).digest()
        return k_signing

    def xǁAwsS3Connectorǁ_get_signing_key__mutmut_24(self, date_stamp: str) -> bytes:
        """Deriva la clave de firma AWS Signature V4.

        Args:
            date_stamp: Fecha en formato YYYYMMDD

        Returns:
            Clave de firma en bytes
        """
        k_date = hmac.new(
            f"AWS4{self._secret_access_key}".encode(),
            date_stamp.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        k_region = hmac.new(k_date, self._region.encode("utf-8"), hashlib.sha256).digest()
        k_service = hmac.new(k_region, b"s3", None).digest()
        k_signing = hmac.new(k_service, b"aws4_request", hashlib.sha256).digest()
        return k_signing

    def xǁAwsS3Connectorǁ_get_signing_key__mutmut_25(self, date_stamp: str) -> bytes:
        """Deriva la clave de firma AWS Signature V4.

        Args:
            date_stamp: Fecha en formato YYYYMMDD

        Returns:
            Clave de firma en bytes
        """
        k_date = hmac.new(
            f"AWS4{self._secret_access_key}".encode(),
            date_stamp.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        k_region = hmac.new(k_date, self._region.encode("utf-8"), hashlib.sha256).digest()
        k_service = hmac.new(b"s3", hashlib.sha256).digest()
        k_signing = hmac.new(k_service, b"aws4_request", hashlib.sha256).digest()
        return k_signing

    def xǁAwsS3Connectorǁ_get_signing_key__mutmut_26(self, date_stamp: str) -> bytes:
        """Deriva la clave de firma AWS Signature V4.

        Args:
            date_stamp: Fecha en formato YYYYMMDD

        Returns:
            Clave de firma en bytes
        """
        k_date = hmac.new(
            f"AWS4{self._secret_access_key}".encode(),
            date_stamp.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        k_region = hmac.new(k_date, self._region.encode("utf-8"), hashlib.sha256).digest()
        k_service = hmac.new(k_region, hashlib.sha256).digest()
        k_signing = hmac.new(k_service, b"aws4_request", hashlib.sha256).digest()
        return k_signing

    def xǁAwsS3Connectorǁ_get_signing_key__mutmut_27(self, date_stamp: str) -> bytes:
        """Deriva la clave de firma AWS Signature V4.

        Args:
            date_stamp: Fecha en formato YYYYMMDD

        Returns:
            Clave de firma en bytes
        """
        k_date = hmac.new(
            f"AWS4{self._secret_access_key}".encode(),
            date_stamp.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        k_region = hmac.new(k_date, self._region.encode("utf-8"), hashlib.sha256).digest()
        k_service = hmac.new(k_region, b"s3", ).digest()
        k_signing = hmac.new(k_service, b"aws4_request", hashlib.sha256).digest()
        return k_signing

    def xǁAwsS3Connectorǁ_get_signing_key__mutmut_28(self, date_stamp: str) -> bytes:
        """Deriva la clave de firma AWS Signature V4.

        Args:
            date_stamp: Fecha en formato YYYYMMDD

        Returns:
            Clave de firma en bytes
        """
        k_date = hmac.new(
            f"AWS4{self._secret_access_key}".encode(),
            date_stamp.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        k_region = hmac.new(k_date, self._region.encode("utf-8"), hashlib.sha256).digest()
        k_service = hmac.new(k_region, b"XXs3XX", hashlib.sha256).digest()
        k_signing = hmac.new(k_service, b"aws4_request", hashlib.sha256).digest()
        return k_signing

    def xǁAwsS3Connectorǁ_get_signing_key__mutmut_29(self, date_stamp: str) -> bytes:
        """Deriva la clave de firma AWS Signature V4.

        Args:
            date_stamp: Fecha en formato YYYYMMDD

        Returns:
            Clave de firma en bytes
        """
        k_date = hmac.new(
            f"AWS4{self._secret_access_key}".encode(),
            date_stamp.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        k_region = hmac.new(k_date, self._region.encode("utf-8"), hashlib.sha256).digest()
        k_service = hmac.new(k_region, b"S3", hashlib.sha256).digest()
        k_signing = hmac.new(k_service, b"aws4_request", hashlib.sha256).digest()
        return k_signing

    def xǁAwsS3Connectorǁ_get_signing_key__mutmut_30(self, date_stamp: str) -> bytes:
        """Deriva la clave de firma AWS Signature V4.

        Args:
            date_stamp: Fecha en formato YYYYMMDD

        Returns:
            Clave de firma en bytes
        """
        k_date = hmac.new(
            f"AWS4{self._secret_access_key}".encode(),
            date_stamp.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        k_region = hmac.new(k_date, self._region.encode("utf-8"), hashlib.sha256).digest()
        k_service = hmac.new(k_region, b"s3", hashlib.sha256).digest()
        k_signing = None
        return k_signing

    def xǁAwsS3Connectorǁ_get_signing_key__mutmut_31(self, date_stamp: str) -> bytes:
        """Deriva la clave de firma AWS Signature V4.

        Args:
            date_stamp: Fecha en formato YYYYMMDD

        Returns:
            Clave de firma en bytes
        """
        k_date = hmac.new(
            f"AWS4{self._secret_access_key}".encode(),
            date_stamp.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        k_region = hmac.new(k_date, self._region.encode("utf-8"), hashlib.sha256).digest()
        k_service = hmac.new(k_region, b"s3", hashlib.sha256).digest()
        k_signing = hmac.new(None, b"aws4_request", hashlib.sha256).digest()
        return k_signing

    def xǁAwsS3Connectorǁ_get_signing_key__mutmut_32(self, date_stamp: str) -> bytes:
        """Deriva la clave de firma AWS Signature V4.

        Args:
            date_stamp: Fecha en formato YYYYMMDD

        Returns:
            Clave de firma en bytes
        """
        k_date = hmac.new(
            f"AWS4{self._secret_access_key}".encode(),
            date_stamp.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        k_region = hmac.new(k_date, self._region.encode("utf-8"), hashlib.sha256).digest()
        k_service = hmac.new(k_region, b"s3", hashlib.sha256).digest()
        k_signing = hmac.new(k_service, None, hashlib.sha256).digest()
        return k_signing

    def xǁAwsS3Connectorǁ_get_signing_key__mutmut_33(self, date_stamp: str) -> bytes:
        """Deriva la clave de firma AWS Signature V4.

        Args:
            date_stamp: Fecha en formato YYYYMMDD

        Returns:
            Clave de firma en bytes
        """
        k_date = hmac.new(
            f"AWS4{self._secret_access_key}".encode(),
            date_stamp.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        k_region = hmac.new(k_date, self._region.encode("utf-8"), hashlib.sha256).digest()
        k_service = hmac.new(k_region, b"s3", hashlib.sha256).digest()
        k_signing = hmac.new(k_service, b"aws4_request", None).digest()
        return k_signing

    def xǁAwsS3Connectorǁ_get_signing_key__mutmut_34(self, date_stamp: str) -> bytes:
        """Deriva la clave de firma AWS Signature V4.

        Args:
            date_stamp: Fecha en formato YYYYMMDD

        Returns:
            Clave de firma en bytes
        """
        k_date = hmac.new(
            f"AWS4{self._secret_access_key}".encode(),
            date_stamp.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        k_region = hmac.new(k_date, self._region.encode("utf-8"), hashlib.sha256).digest()
        k_service = hmac.new(k_region, b"s3", hashlib.sha256).digest()
        k_signing = hmac.new(b"aws4_request", hashlib.sha256).digest()
        return k_signing

    def xǁAwsS3Connectorǁ_get_signing_key__mutmut_35(self, date_stamp: str) -> bytes:
        """Deriva la clave de firma AWS Signature V4.

        Args:
            date_stamp: Fecha en formato YYYYMMDD

        Returns:
            Clave de firma en bytes
        """
        k_date = hmac.new(
            f"AWS4{self._secret_access_key}".encode(),
            date_stamp.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        k_region = hmac.new(k_date, self._region.encode("utf-8"), hashlib.sha256).digest()
        k_service = hmac.new(k_region, b"s3", hashlib.sha256).digest()
        k_signing = hmac.new(k_service, hashlib.sha256).digest()
        return k_signing

    def xǁAwsS3Connectorǁ_get_signing_key__mutmut_36(self, date_stamp: str) -> bytes:
        """Deriva la clave de firma AWS Signature V4.

        Args:
            date_stamp: Fecha en formato YYYYMMDD

        Returns:
            Clave de firma en bytes
        """
        k_date = hmac.new(
            f"AWS4{self._secret_access_key}".encode(),
            date_stamp.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        k_region = hmac.new(k_date, self._region.encode("utf-8"), hashlib.sha256).digest()
        k_service = hmac.new(k_region, b"s3", hashlib.sha256).digest()
        k_signing = hmac.new(k_service, b"aws4_request", ).digest()
        return k_signing

    def xǁAwsS3Connectorǁ_get_signing_key__mutmut_37(self, date_stamp: str) -> bytes:
        """Deriva la clave de firma AWS Signature V4.

        Args:
            date_stamp: Fecha en formato YYYYMMDD

        Returns:
            Clave de firma en bytes
        """
        k_date = hmac.new(
            f"AWS4{self._secret_access_key}".encode(),
            date_stamp.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        k_region = hmac.new(k_date, self._region.encode("utf-8"), hashlib.sha256).digest()
        k_service = hmac.new(k_region, b"s3", hashlib.sha256).digest()
        k_signing = hmac.new(k_service, b"XXaws4_requestXX", hashlib.sha256).digest()
        return k_signing

    def xǁAwsS3Connectorǁ_get_signing_key__mutmut_38(self, date_stamp: str) -> bytes:
        """Deriva la clave de firma AWS Signature V4.

        Args:
            date_stamp: Fecha en formato YYYYMMDD

        Returns:
            Clave de firma en bytes
        """
        k_date = hmac.new(
            f"AWS4{self._secret_access_key}".encode(),
            date_stamp.encode("utf-8"),
            hashlib.sha256,
        ).digest()
        k_region = hmac.new(k_date, self._region.encode("utf-8"), hashlib.sha256).digest()
        k_service = hmac.new(k_region, b"s3", hashlib.sha256).digest()
        k_signing = hmac.new(k_service, b"AWS4_REQUEST", hashlib.sha256).digest()
        return k_signing

mutants_xǁAwsS3Connectorǁ__init____mutmut['_mutmut_orig'] = AwsS3Connector.xǁAwsS3Connectorǁ__init____mutmut_orig # type: ignore # mutmut generated
mutants_xǁAwsS3Connectorǁ