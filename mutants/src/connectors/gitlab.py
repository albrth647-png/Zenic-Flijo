"""
Conector GitLab — CI/CD y Proyectos via GitLab API
=======================================================

Permite gestionar proyectos, pipelines, jobs y merge
requests via la GitLab REST API.
"""

from __future__ import annotations

from typing import Any

from src.core.logging import setup_logging
from src.sdk.base import BaseConnector
from src.sdk.http_client import HttpClient, HTTPClientError
from src.sdk.schema import ActionDefinition, AuthRequirement, ConnectorSchema

logger = setup_logging(__name__)


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_xǁGitlabConnectorǁ__init____mutmut: MutantDict = {}  # type: ignore
mutants_xǁGitlabConnectorǁ_get_token__mutmut: MutantDict = {}  # type: ignore
mutants_xǁGitlabConnectorǁconnect__mutmut: MutantDict = {}  # type: ignore
mutants_xǁGitlabConnectorǁexecute__mutmut: MutantDict = {}  # type: ignore
mutants_xǁGitlabConnectorǁvalidate__mutmut: MutantDict = {}  # type: ignore
mutants_xǁGitlabConnectorǁdisconnect__mutmut: MutantDict = {}  # type: ignore
mutants_xǁGitlabConnectorǁ_list_projects__mutmut: MutantDict = {}  # type: ignore
mutants_xǁGitlabConnectorǁ_get_project__mutmut: MutantDict = {}  # type: ignore
mutants_xǁGitlabConnectorǁ_list_pipelines__mutmut: MutantDict = {}  # type: ignore
mutants_xǁGitlabConnectorǁ_trigger_pipeline__mutmut: MutantDict = {}  # type: ignore
mutants_xǁGitlabConnectorǁ_create_merge_request__mutmut: MutantDict = {}  # type: ignore
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut: MutantDict = {}  # type: ignore


class GitlabConnector(BaseConnector):
    """Conector para GitLab: proyectos, pipelines y merge requests."""

    name = "gitlab"
    version = "1.0.0"
    description = "Gestiona proyectos, pipelines y merge requests via GitLab API"
    category = "devops_monitoring"
    icon = "git-merge"
    author = "Zenic-Flijo"

    @_mutmut_mutated(mutants_xǁGitlabConnectorǁ__init____mutmut)
    def __init__(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://gitlab.com/api/v4"
        self._http: HttpClient | None = None

    def xǁGitlabConnectorǁ__init____mutmut_orig(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://gitlab.com/api/v4"
        self._http: HttpClient | None = None

    def xǁGitlabConnectorǁ__init____mutmut_1(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = None
        self._http: HttpClient | None = None

    def xǁGitlabConnectorǁ__init____mutmut_2(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "XXhttps://gitlab.com/api/v4XX"
        self._http: HttpClient | None = None

    def xǁGitlabConnectorǁ__init____mutmut_3(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "HTTPS://GITLAB.COM/API/V4"
        self._http: HttpClient | None = None

    def xǁGitlabConnectorǁ__init____mutmut_4(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://gitlab.com/api/v4"
        self._http: HttpClient | None = ""

    @_mutmut_mutated(mutants_xǁGitlabConnectorǁ_get_token__mutmut)
    def _get_token(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_orig(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_1(self) -> str:
        """Extract the private token from the auth provider."""
        if self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_2(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return "XXXX"
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_3(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = None
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_4(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"XXheadersXX": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_5(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"HEADERS": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_6(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "XXparamsXX": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_7(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "PARAMS": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_8(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(None)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_9(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = None
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_10(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get(None, "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_11(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", None)
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_12(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_13(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", )
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_14(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["XXheadersXX"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_15(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["HEADERS"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_16(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("XXAuthorizationXX", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_17(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_18(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("AUTHORIZATION", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_19(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "XXXX")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_20(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith(None):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_21(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("XXBearer XX"):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_22(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_23(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("BEARER "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_24(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[8:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_25(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith(None):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_26(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("XXToken XX"):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_27(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_28(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("TOKEN "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_29(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[7:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_30(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = None
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_31(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get(None, "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_32(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", None)
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_33(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_34(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", )
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_35(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["XXheadersXX"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_36(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["HEADERS"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_37(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("XXX-API-KeyXX", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_38(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("x-api-key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_39(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-KEY", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_40(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "XXXX")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_41(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("XX_api_keyXX", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_42(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_API_KEY", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_43(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "XX_tokenXX", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_44(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_TOKEN", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_45(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "XX_access_tokenXX", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_46(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_ACCESS_TOKEN", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_47(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "XX_private_tokenXX"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_48(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_PRIVATE_TOKEN"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_49(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(None, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_50(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, None):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_51(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_52(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, ):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_53(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = None
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_54(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(None, attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_55(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, None, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_56(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, None)
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_57(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(attr, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_58(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, "")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_59(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, )
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_60(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "XXXX")
                if isinstance(val, str) and val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_61(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) or val:
                    return val
        return ""

    def xǁGitlabConnectorǁ_get_token__mutmut_62(self) -> str:
        """Extract the private token from the auth provider."""
        if not self._auth_provider:
            return ""
        # Use apply_auth on a dummy request to extract credentials
        request: dict[str, Any] = {"headers": {}, "params": {}}
        self._auth_provider.apply_auth(request)
        # Check for Authorization header (Bearer or Token)
        auth_header = request["headers"].get("Authorization", "")
        if auth_header.startswith("Bearer "):
            return auth_header[7:]
        if auth_header.startswith("Token "):
            return auth_header[6:]
        # Try X-API-Key header
        api_key = request["headers"].get("X-API-Key", "")
        if api_key:
            return api_key
        # Try direct attribute access
        for attr in ("_api_key", "_token", "_access_token", "_private_token"):
            if hasattr(self._auth_provider, attr):
                val = getattr(self._auth_provider, attr, "")
                if isinstance(val, str) and val:
                    return val
        return "XXXX"

    @_mutmut_mutated(mutants_xǁGitlabConnectorǁconnect__mutmut)
    def connect(self) -> bool:
        """Establece conexion con la GitLab API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GitlabConnector: Private Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GitlabConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitLab uses PRIVATE-TOKEN header
        self._http.set_header("PRIVATE-TOKEN", token)

        # Validate credentials by fetching current user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Private Token configurado y validado")
                return True
            else:
                logger.error(f"GitlabConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GitlabConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGitlabConnectorǁconnect__mutmut_orig(self) -> bool:
        """Establece conexion con la GitLab API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GitlabConnector: Private Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GitlabConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitLab uses PRIVATE-TOKEN header
        self._http.set_header("PRIVATE-TOKEN", token)

        # Validate credentials by fetching current user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Private Token configurado y validado")
                return True
            else:
                logger.error(f"GitlabConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GitlabConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGitlabConnectorǁconnect__mutmut_1(self) -> bool:
        """Establece conexion con la GitLab API."""
        if not self._auth_provider and not self._auth_provider.validate():
            logger.error("GitlabConnector: Private Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GitlabConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitLab uses PRIVATE-TOKEN header
        self._http.set_header("PRIVATE-TOKEN", token)

        # Validate credentials by fetching current user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Private Token configurado y validado")
                return True
            else:
                logger.error(f"GitlabConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GitlabConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGitlabConnectorǁconnect__mutmut_2(self) -> bool:
        """Establece conexion con la GitLab API."""
        if self._auth_provider or not self._auth_provider.validate():
            logger.error("GitlabConnector: Private Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GitlabConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitLab uses PRIVATE-TOKEN header
        self._http.set_header("PRIVATE-TOKEN", token)

        # Validate credentials by fetching current user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Private Token configurado y validado")
                return True
            else:
                logger.error(f"GitlabConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GitlabConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGitlabConnectorǁconnect__mutmut_3(self) -> bool:
        """Establece conexion con la GitLab API."""
        if not self._auth_provider or self._auth_provider.validate():
            logger.error("GitlabConnector: Private Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GitlabConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitLab uses PRIVATE-TOKEN header
        self._http.set_header("PRIVATE-TOKEN", token)

        # Validate credentials by fetching current user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Private Token configurado y validado")
                return True
            else:
                logger.error(f"GitlabConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GitlabConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGitlabConnectorǁconnect__mutmut_4(self) -> bool:
        """Establece conexion con la GitLab API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error(None)
            return False

        token = self._get_token()
        if not token:
            logger.error("GitlabConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitLab uses PRIVATE-TOKEN header
        self._http.set_header("PRIVATE-TOKEN", token)

        # Validate credentials by fetching current user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Private Token configurado y validado")
                return True
            else:
                logger.error(f"GitlabConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GitlabConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGitlabConnectorǁconnect__mutmut_5(self) -> bool:
        """Establece conexion con la GitLab API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("XXGitlabConnector: Private Token no configuradoXX")
            return False

        token = self._get_token()
        if not token:
            logger.error("GitlabConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitLab uses PRIVATE-TOKEN header
        self._http.set_header("PRIVATE-TOKEN", token)

        # Validate credentials by fetching current user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Private Token configurado y validado")
                return True
            else:
                logger.error(f"GitlabConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GitlabConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGitlabConnectorǁconnect__mutmut_6(self) -> bool:
        """Establece conexion con la GitLab API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("gitlabconnector: private token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GitlabConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitLab uses PRIVATE-TOKEN header
        self._http.set_header("PRIVATE-TOKEN", token)

        # Validate credentials by fetching current user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Private Token configurado y validado")
                return True
            else:
                logger.error(f"GitlabConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GitlabConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGitlabConnectorǁconnect__mutmut_7(self) -> bool:
        """Establece conexion con la GitLab API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GITLABCONNECTOR: PRIVATE TOKEN NO CONFIGURADO")
            return False

        token = self._get_token()
        if not token:
            logger.error("GitlabConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitLab uses PRIVATE-TOKEN header
        self._http.set_header("PRIVATE-TOKEN", token)

        # Validate credentials by fetching current user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Private Token configurado y validado")
                return True
            else:
                logger.error(f"GitlabConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GitlabConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGitlabConnectorǁconnect__mutmut_8(self) -> bool:
        """Establece conexion con la GitLab API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GitlabConnector: Private Token no configurado")
            return True

        token = self._get_token()
        if not token:
            logger.error("GitlabConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitLab uses PRIVATE-TOKEN header
        self._http.set_header("PRIVATE-TOKEN", token)

        # Validate credentials by fetching current user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Private Token configurado y validado")
                return True
            else:
                logger.error(f"GitlabConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GitlabConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGitlabConnectorǁconnect__mutmut_9(self) -> bool:
        """Establece conexion con la GitLab API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GitlabConnector: Private Token no configurado")
            return False

        token = None
        if not token:
            logger.error("GitlabConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitLab uses PRIVATE-TOKEN header
        self._http.set_header("PRIVATE-TOKEN", token)

        # Validate credentials by fetching current user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Private Token configurado y validado")
                return True
            else:
                logger.error(f"GitlabConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GitlabConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGitlabConnectorǁconnect__mutmut_10(self) -> bool:
        """Establece conexion con la GitLab API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GitlabConnector: Private Token no configurado")
            return False

        token = self._get_token()
        if token:
            logger.error("GitlabConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitLab uses PRIVATE-TOKEN header
        self._http.set_header("PRIVATE-TOKEN", token)

        # Validate credentials by fetching current user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Private Token configurado y validado")
                return True
            else:
                logger.error(f"GitlabConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GitlabConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGitlabConnectorǁconnect__mutmut_11(self) -> bool:
        """Establece conexion con la GitLab API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GitlabConnector: Private Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error(None)
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitLab uses PRIVATE-TOKEN header
        self._http.set_header("PRIVATE-TOKEN", token)

        # Validate credentials by fetching current user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Private Token configurado y validado")
                return True
            else:
                logger.error(f"GitlabConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GitlabConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGitlabConnectorǁconnect__mutmut_12(self) -> bool:
        """Establece conexion con la GitLab API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GitlabConnector: Private Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("XXGitlabConnector: No se pudo extraer el token del auth providerXX")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitLab uses PRIVATE-TOKEN header
        self._http.set_header("PRIVATE-TOKEN", token)

        # Validate credentials by fetching current user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Private Token configurado y validado")
                return True
            else:
                logger.error(f"GitlabConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GitlabConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGitlabConnectorǁconnect__mutmut_13(self) -> bool:
        """Establece conexion con la GitLab API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GitlabConnector: Private Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("gitlabconnector: no se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitLab uses PRIVATE-TOKEN header
        self._http.set_header("PRIVATE-TOKEN", token)

        # Validate credentials by fetching current user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Private Token configurado y validado")
                return True
            else:
                logger.error(f"GitlabConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GitlabConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGitlabConnectorǁconnect__mutmut_14(self) -> bool:
        """Establece conexion con la GitLab API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GitlabConnector: Private Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GITLABCONNECTOR: NO SE PUDO EXTRAER EL TOKEN DEL AUTH PROVIDER")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitLab uses PRIVATE-TOKEN header
        self._http.set_header("PRIVATE-TOKEN", token)

        # Validate credentials by fetching current user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Private Token configurado y validado")
                return True
            else:
                logger.error(f"GitlabConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GitlabConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGitlabConnectorǁconnect__mutmut_15(self) -> bool:
        """Establece conexion con la GitLab API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GitlabConnector: Private Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GitlabConnector: No se pudo extraer el token del auth provider")
            return True

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitLab uses PRIVATE-TOKEN header
        self._http.set_header("PRIVATE-TOKEN", token)

        # Validate credentials by fetching current user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Private Token configurado y validado")
                return True
            else:
                logger.error(f"GitlabConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GitlabConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGitlabConnectorǁconnect__mutmut_16(self) -> bool:
        """Establece conexion con la GitLab API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GitlabConnector: Private Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GitlabConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = None
        # GitLab uses PRIVATE-TOKEN header
        self._http.set_header("PRIVATE-TOKEN", token)

        # Validate credentials by fetching current user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Private Token configurado y validado")
                return True
            else:
                logger.error(f"GitlabConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GitlabConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGitlabConnectorǁconnect__mutmut_17(self) -> bool:
        """Establece conexion con la GitLab API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GitlabConnector: Private Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GitlabConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=None, connector_name=self.name)
        # GitLab uses PRIVATE-TOKEN header
        self._http.set_header("PRIVATE-TOKEN", token)

        # Validate credentials by fetching current user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Private Token configurado y validado")
                return True
            else:
                logger.error(f"GitlabConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GitlabConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGitlabConnectorǁconnect__mutmut_18(self) -> bool:
        """Establece conexion con la GitLab API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GitlabConnector: Private Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GitlabConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=None)
        # GitLab uses PRIVATE-TOKEN header
        self._http.set_header("PRIVATE-TOKEN", token)

        # Validate credentials by fetching current user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Private Token configurado y validado")
                return True
            else:
                logger.error(f"GitlabConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GitlabConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGitlabConnectorǁconnect__mutmut_19(self) -> bool:
        """Establece conexion con la GitLab API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GitlabConnector: Private Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GitlabConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(connector_name=self.name)
        # GitLab uses PRIVATE-TOKEN header
        self._http.set_header("PRIVATE-TOKEN", token)

        # Validate credentials by fetching current user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Private Token configurado y validado")
                return True
            else:
                logger.error(f"GitlabConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GitlabConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGitlabConnectorǁconnect__mutmut_20(self) -> bool:
        """Establece conexion con la GitLab API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GitlabConnector: Private Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GitlabConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, )
        # GitLab uses PRIVATE-TOKEN header
        self._http.set_header("PRIVATE-TOKEN", token)

        # Validate credentials by fetching current user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Private Token configurado y validado")
                return True
            else:
                logger.error(f"GitlabConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GitlabConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGitlabConnectorǁconnect__mutmut_21(self) -> bool:
        """Establece conexion con la GitLab API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GitlabConnector: Private Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GitlabConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitLab uses PRIVATE-TOKEN header
        self._http.set_header(None, token)

        # Validate credentials by fetching current user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Private Token configurado y validado")
                return True
            else:
                logger.error(f"GitlabConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GitlabConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGitlabConnectorǁconnect__mutmut_22(self) -> bool:
        """Establece conexion con la GitLab API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GitlabConnector: Private Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GitlabConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitLab uses PRIVATE-TOKEN header
        self._http.set_header("PRIVATE-TOKEN", None)

        # Validate credentials by fetching current user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Private Token configurado y validado")
                return True
            else:
                logger.error(f"GitlabConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GitlabConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGitlabConnectorǁconnect__mutmut_23(self) -> bool:
        """Establece conexion con la GitLab API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GitlabConnector: Private Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GitlabConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitLab uses PRIVATE-TOKEN header
        self._http.set_header(token)

        # Validate credentials by fetching current user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Private Token configurado y validado")
                return True
            else:
                logger.error(f"GitlabConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GitlabConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGitlabConnectorǁconnect__mutmut_24(self) -> bool:
        """Establece conexion con la GitLab API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GitlabConnector: Private Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GitlabConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitLab uses PRIVATE-TOKEN header
        self._http.set_header("PRIVATE-TOKEN", )

        # Validate credentials by fetching current user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Private Token configurado y validado")
                return True
            else:
                logger.error(f"GitlabConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GitlabConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGitlabConnectorǁconnect__mutmut_25(self) -> bool:
        """Establece conexion con la GitLab API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GitlabConnector: Private Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GitlabConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitLab uses PRIVATE-TOKEN header
        self._http.set_header("XXPRIVATE-TOKENXX", token)

        # Validate credentials by fetching current user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Private Token configurado y validado")
                return True
            else:
                logger.error(f"GitlabConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GitlabConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGitlabConnectorǁconnect__mutmut_26(self) -> bool:
        """Establece conexion con la GitLab API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GitlabConnector: Private Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GitlabConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitLab uses PRIVATE-TOKEN header
        self._http.set_header("private-token", token)

        # Validate credentials by fetching current user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Private Token configurado y validado")
                return True
            else:
                logger.error(f"GitlabConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GitlabConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGitlabConnectorǁconnect__mutmut_27(self) -> bool:
        """Establece conexion con la GitLab API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GitlabConnector: Private Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GitlabConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitLab uses PRIVATE-TOKEN header
        self._http.set_header("PRIVATE-TOKEN", token)

        # Validate credentials by fetching current user
        try:
            resp = None
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Private Token configurado y validado")
                return True
            else:
                logger.error(f"GitlabConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GitlabConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGitlabConnectorǁconnect__mutmut_28(self) -> bool:
        """Establece conexion con la GitLab API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GitlabConnector: Private Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GitlabConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitLab uses PRIVATE-TOKEN header
        self._http.set_header("PRIVATE-TOKEN", token)

        # Validate credentials by fetching current user
        try:
            resp = self._http.get(None)
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Private Token configurado y validado")
                return True
            else:
                logger.error(f"GitlabConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GitlabConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGitlabConnectorǁconnect__mutmut_29(self) -> bool:
        """Establece conexion con la GitLab API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GitlabConnector: Private Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GitlabConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitLab uses PRIVATE-TOKEN header
        self._http.set_header("PRIVATE-TOKEN", token)

        # Validate credentials by fetching current user
        try:
            resp = self._http.get("XX/userXX")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Private Token configurado y validado")
                return True
            else:
                logger.error(f"GitlabConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GitlabConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGitlabConnectorǁconnect__mutmut_30(self) -> bool:
        """Establece conexion con la GitLab API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GitlabConnector: Private Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GitlabConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitLab uses PRIVATE-TOKEN header
        self._http.set_header("PRIVATE-TOKEN", token)

        # Validate credentials by fetching current user
        try:
            resp = self._http.get("/USER")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Private Token configurado y validado")
                return True
            else:
                logger.error(f"GitlabConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GitlabConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGitlabConnectorǁconnect__mutmut_31(self) -> bool:
        """Establece conexion con la GitLab API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GitlabConnector: Private Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GitlabConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitLab uses PRIVATE-TOKEN header
        self._http.set_header("PRIVATE-TOKEN", token)

        # Validate credentials by fetching current user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = None
                self._log_operation("connect", "Private Token configurado y validado")
                return True
            else:
                logger.error(f"GitlabConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GitlabConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGitlabConnectorǁconnect__mutmut_32(self) -> bool:
        """Establece conexion con la GitLab API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GitlabConnector: Private Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GitlabConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitLab uses PRIVATE-TOKEN header
        self._http.set_header("PRIVATE-TOKEN", token)

        # Validate credentials by fetching current user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = False
                self._log_operation("connect", "Private Token configurado y validado")
                return True
            else:
                logger.error(f"GitlabConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GitlabConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGitlabConnectorǁconnect__mutmut_33(self) -> bool:
        """Establece conexion con la GitLab API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GitlabConnector: Private Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GitlabConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitLab uses PRIVATE-TOKEN header
        self._http.set_header("PRIVATE-TOKEN", token)

        # Validate credentials by fetching current user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation(None, "Private Token configurado y validado")
                return True
            else:
                logger.error(f"GitlabConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GitlabConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGitlabConnectorǁconnect__mutmut_34(self) -> bool:
        """Establece conexion con la GitLab API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GitlabConnector: Private Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GitlabConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitLab uses PRIVATE-TOKEN header
        self._http.set_header("PRIVATE-TOKEN", token)

        # Validate credentials by fetching current user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", None)
                return True
            else:
                logger.error(f"GitlabConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GitlabConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGitlabConnectorǁconnect__mutmut_35(self) -> bool:
        """Establece conexion con la GitLab API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GitlabConnector: Private Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GitlabConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitLab uses PRIVATE-TOKEN header
        self._http.set_header("PRIVATE-TOKEN", token)

        # Validate credentials by fetching current user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("Private Token configurado y validado")
                return True
            else:
                logger.error(f"GitlabConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GitlabConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGitlabConnectorǁconnect__mutmut_36(self) -> bool:
        """Establece conexion con la GitLab API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GitlabConnector: Private Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GitlabConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitLab uses PRIVATE-TOKEN header
        self._http.set_header("PRIVATE-TOKEN", token)

        # Validate credentials by fetching current user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", )
                return True
            else:
                logger.error(f"GitlabConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GitlabConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGitlabConnectorǁconnect__mutmut_37(self) -> bool:
        """Establece conexion con la GitLab API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GitlabConnector: Private Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GitlabConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitLab uses PRIVATE-TOKEN header
        self._http.set_header("PRIVATE-TOKEN", token)

        # Validate credentials by fetching current user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("XXconnectXX", "Private Token configurado y validado")
                return True
            else:
                logger.error(f"GitlabConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GitlabConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGitlabConnectorǁconnect__mutmut_38(self) -> bool:
        """Establece conexion con la GitLab API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GitlabConnector: Private Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GitlabConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitLab uses PRIVATE-TOKEN header
        self._http.set_header("PRIVATE-TOKEN", token)

        # Validate credentials by fetching current user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("CONNECT", "Private Token configurado y validado")
                return True
            else:
                logger.error(f"GitlabConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GitlabConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGitlabConnectorǁconnect__mutmut_39(self) -> bool:
        """Establece conexion con la GitLab API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GitlabConnector: Private Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GitlabConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitLab uses PRIVATE-TOKEN header
        self._http.set_header("PRIVATE-TOKEN", token)

        # Validate credentials by fetching current user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "XXPrivate Token configurado y validadoXX")
                return True
            else:
                logger.error(f"GitlabConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GitlabConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGitlabConnectorǁconnect__mutmut_40(self) -> bool:
        """Establece conexion con la GitLab API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GitlabConnector: Private Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GitlabConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitLab uses PRIVATE-TOKEN header
        self._http.set_header("PRIVATE-TOKEN", token)

        # Validate credentials by fetching current user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "private token configurado y validado")
                return True
            else:
                logger.error(f"GitlabConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GitlabConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGitlabConnectorǁconnect__mutmut_41(self) -> bool:
        """Establece conexion con la GitLab API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GitlabConnector: Private Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GitlabConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitLab uses PRIVATE-TOKEN header
        self._http.set_header("PRIVATE-TOKEN", token)

        # Validate credentials by fetching current user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "PRIVATE TOKEN CONFIGURADO Y VALIDADO")
                return True
            else:
                logger.error(f"GitlabConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GitlabConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGitlabConnectorǁconnect__mutmut_42(self) -> bool:
        """Establece conexion con la GitLab API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GitlabConnector: Private Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GitlabConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitLab uses PRIVATE-TOKEN header
        self._http.set_header("PRIVATE-TOKEN", token)

        # Validate credentials by fetching current user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Private Token configurado y validado")
                return False
            else:
                logger.error(f"GitlabConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GitlabConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGitlabConnectorǁconnect__mutmut_43(self) -> bool:
        """Establece conexion con la GitLab API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GitlabConnector: Private Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GitlabConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitLab uses PRIVATE-TOKEN header
        self._http.set_header("PRIVATE-TOKEN", token)

        # Validate credentials by fetching current user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Private Token configurado y validado")
                return True
            else:
                logger.error(None)
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GitlabConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGitlabConnectorǁconnect__mutmut_44(self) -> bool:
        """Establece conexion con la GitLab API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GitlabConnector: Private Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GitlabConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitLab uses PRIVATE-TOKEN header
        self._http.set_header("PRIVATE-TOKEN", token)

        # Validate credentials by fetching current user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Private Token configurado y validado")
                return True
            else:
                logger.error(f"GitlabConnector: Validacion de token fallida - {resp.status_code}")
                self._http = ""
                return False
        except HTTPClientError as e:
            logger.error(f"GitlabConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGitlabConnectorǁconnect__mutmut_45(self) -> bool:
        """Establece conexion con la GitLab API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GitlabConnector: Private Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GitlabConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitLab uses PRIVATE-TOKEN header
        self._http.set_header("PRIVATE-TOKEN", token)

        # Validate credentials by fetching current user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Private Token configurado y validado")
                return True
            else:
                logger.error(f"GitlabConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return True
        except HTTPClientError as e:
            logger.error(f"GitlabConnector: Error validando token - {e}")
            self._http = None
            return False

    def xǁGitlabConnectorǁconnect__mutmut_46(self) -> bool:
        """Establece conexion con la GitLab API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GitlabConnector: Private Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GitlabConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitLab uses PRIVATE-TOKEN header
        self._http.set_header("PRIVATE-TOKEN", token)

        # Validate credentials by fetching current user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Private Token configurado y validado")
                return True
            else:
                logger.error(f"GitlabConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(None)
            self._http = None
            return False

    def xǁGitlabConnectorǁconnect__mutmut_47(self) -> bool:
        """Establece conexion con la GitLab API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GitlabConnector: Private Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GitlabConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitLab uses PRIVATE-TOKEN header
        self._http.set_header("PRIVATE-TOKEN", token)

        # Validate credentials by fetching current user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Private Token configurado y validado")
                return True
            else:
                logger.error(f"GitlabConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GitlabConnector: Error validando token - {e}")
            self._http = ""
            return False

    def xǁGitlabConnectorǁconnect__mutmut_48(self) -> bool:
        """Establece conexion con la GitLab API."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("GitlabConnector: Private Token no configurado")
            return False

        token = self._get_token()
        if not token:
            logger.error("GitlabConnector: No se pudo extraer el token del auth provider")
            return False

        self._http = HttpClient(base_url=self._base_url, connector_name=self.name)
        # GitLab uses PRIVATE-TOKEN header
        self._http.set_header("PRIVATE-TOKEN", token)

        # Validate credentials by fetching current user
        try:
            resp = self._http.get("/user")
            if resp.ok:
                self._connected = True
                self._log_operation("connect", "Private Token configurado y validado")
                return True
            else:
                logger.error(f"GitlabConnector: Validacion de token fallida - {resp.status_code}")
                self._http = None
                return False
        except HTTPClientError as e:
            logger.error(f"GitlabConnector: Error validando token - {e}")
            self._http = None
            return True

    @_mutmut_mutated(mutants_xǁGitlabConnectorǁexecute__mutmut)
    def execute(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GitLab.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_projects": self._list_projects,
            "get_project": self._get_project,
            "list_pipelines": self._list_pipelines,
            "trigger_pipeline": self._trigger_pipeline,
            "create_merge_request": self._create_merge_request,
            "list_merge_requests": self._list_merge_requests,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGitlabConnectorǁexecute__mutmut_orig(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GitLab.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_projects": self._list_projects,
            "get_project": self._get_project,
            "list_pipelines": self._list_pipelines,
            "trigger_pipeline": self._trigger_pipeline,
            "create_merge_request": self._create_merge_request,
            "list_merge_requests": self._list_merge_requests,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGitlabConnectorǁexecute__mutmut_1(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GitLab.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = None
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGitlabConnectorǁexecute__mutmut_2(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GitLab.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "XXlist_projectsXX": self._list_projects,
            "get_project": self._get_project,
            "list_pipelines": self._list_pipelines,
            "trigger_pipeline": self._trigger_pipeline,
            "create_merge_request": self._create_merge_request,
            "list_merge_requests": self._list_merge_requests,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGitlabConnectorǁexecute__mutmut_3(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GitLab.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "LIST_PROJECTS": self._list_projects,
            "get_project": self._get_project,
            "list_pipelines": self._list_pipelines,
            "trigger_pipeline": self._trigger_pipeline,
            "create_merge_request": self._create_merge_request,
            "list_merge_requests": self._list_merge_requests,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGitlabConnectorǁexecute__mutmut_4(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GitLab.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_projects": self._list_projects,
            "XXget_projectXX": self._get_project,
            "list_pipelines": self._list_pipelines,
            "trigger_pipeline": self._trigger_pipeline,
            "create_merge_request": self._create_merge_request,
            "list_merge_requests": self._list_merge_requests,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGitlabConnectorǁexecute__mutmut_5(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GitLab.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_projects": self._list_projects,
            "GET_PROJECT": self._get_project,
            "list_pipelines": self._list_pipelines,
            "trigger_pipeline": self._trigger_pipeline,
            "create_merge_request": self._create_merge_request,
            "list_merge_requests": self._list_merge_requests,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGitlabConnectorǁexecute__mutmut_6(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GitLab.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_projects": self._list_projects,
            "get_project": self._get_project,
            "XXlist_pipelinesXX": self._list_pipelines,
            "trigger_pipeline": self._trigger_pipeline,
            "create_merge_request": self._create_merge_request,
            "list_merge_requests": self._list_merge_requests,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGitlabConnectorǁexecute__mutmut_7(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GitLab.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_projects": self._list_projects,
            "get_project": self._get_project,
            "LIST_PIPELINES": self._list_pipelines,
            "trigger_pipeline": self._trigger_pipeline,
            "create_merge_request": self._create_merge_request,
            "list_merge_requests": self._list_merge_requests,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGitlabConnectorǁexecute__mutmut_8(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GitLab.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_projects": self._list_projects,
            "get_project": self._get_project,
            "list_pipelines": self._list_pipelines,
            "XXtrigger_pipelineXX": self._trigger_pipeline,
            "create_merge_request": self._create_merge_request,
            "list_merge_requests": self._list_merge_requests,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGitlabConnectorǁexecute__mutmut_9(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GitLab.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_projects": self._list_projects,
            "get_project": self._get_project,
            "list_pipelines": self._list_pipelines,
            "TRIGGER_PIPELINE": self._trigger_pipeline,
            "create_merge_request": self._create_merge_request,
            "list_merge_requests": self._list_merge_requests,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGitlabConnectorǁexecute__mutmut_10(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GitLab.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_projects": self._list_projects,
            "get_project": self._get_project,
            "list_pipelines": self._list_pipelines,
            "trigger_pipeline": self._trigger_pipeline,
            "XXcreate_merge_requestXX": self._create_merge_request,
            "list_merge_requests": self._list_merge_requests,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGitlabConnectorǁexecute__mutmut_11(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GitLab.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_projects": self._list_projects,
            "get_project": self._get_project,
            "list_pipelines": self._list_pipelines,
            "trigger_pipeline": self._trigger_pipeline,
            "CREATE_MERGE_REQUEST": self._create_merge_request,
            "list_merge_requests": self._list_merge_requests,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGitlabConnectorǁexecute__mutmut_12(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GitLab.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_projects": self._list_projects,
            "get_project": self._get_project,
            "list_pipelines": self._list_pipelines,
            "trigger_pipeline": self._trigger_pipeline,
            "create_merge_request": self._create_merge_request,
            "XXlist_merge_requestsXX": self._list_merge_requests,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGitlabConnectorǁexecute__mutmut_13(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GitLab.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_projects": self._list_projects,
            "get_project": self._get_project,
            "list_pipelines": self._list_pipelines,
            "trigger_pipeline": self._trigger_pipeline,
            "create_merge_request": self._create_merge_request,
            "LIST_MERGE_REQUESTS": self._list_merge_requests,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGitlabConnectorǁexecute__mutmut_14(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GitLab.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_projects": self._list_projects,
            "get_project": self._get_project,
            "list_pipelines": self._list_pipelines,
            "trigger_pipeline": self._trigger_pipeline,
            "create_merge_request": self._create_merge_request,
            "list_merge_requests": self._list_merge_requests,
        }
        handler = None
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGitlabConnectorǁexecute__mutmut_15(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GitLab.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_projects": self._list_projects,
            "get_project": self._get_project,
            "list_pipelines": self._list_pipelines,
            "trigger_pipeline": self._trigger_pipeline,
            "create_merge_request": self._create_merge_request,
            "list_merge_requests": self._list_merge_requests,
        }
        handler = action_map.get(None)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGitlabConnectorǁexecute__mutmut_16(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GitLab.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_projects": self._list_projects,
            "get_project": self._get_project,
            "list_pipelines": self._list_pipelines,
            "trigger_pipeline": self._trigger_pipeline,
            "create_merge_request": self._create_merge_request,
            "list_merge_requests": self._list_merge_requests,
        }
        handler = action_map.get(action)
        if handler is not None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGitlabConnectorǁexecute__mutmut_17(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GitLab.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_projects": self._list_projects,
            "get_project": self._get_project,
            "list_pipelines": self._list_pipelines,
            "trigger_pipeline": self._trigger_pipeline,
            "create_merge_request": self._create_merge_request,
            "list_merge_requests": self._list_merge_requests,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"XXerrorXX": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGitlabConnectorǁexecute__mutmut_18(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GitLab.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_projects": self._list_projects,
            "get_project": self._get_project,
            "list_pipelines": self._list_pipelines,
            "trigger_pipeline": self._trigger_pipeline,
            "create_merge_request": self._create_merge_request,
            "list_merge_requests": self._list_merge_requests,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"ERROR": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁGitlabConnectorǁexecute__mutmut_19(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GitLab.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_projects": self._list_projects,
            "get_project": self._get_project,
            "list_pipelines": self._list_pipelines,
            "trigger_pipeline": self._trigger_pipeline,
            "create_merge_request": self._create_merge_request,
            "list_merge_requests": self._list_merge_requests,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "XXavailableXX": list(action_map.keys())}
        return handler(params)

    def xǁGitlabConnectorǁexecute__mutmut_20(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GitLab.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_projects": self._list_projects,
            "get_project": self._get_project,
            "list_pipelines": self._list_pipelines,
            "trigger_pipeline": self._trigger_pipeline,
            "create_merge_request": self._create_merge_request,
            "list_merge_requests": self._list_merge_requests,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "AVAILABLE": list(action_map.keys())}
        return handler(params)

    def xǁGitlabConnectorǁexecute__mutmut_21(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GitLab.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_projects": self._list_projects,
            "get_project": self._get_project,
            "list_pipelines": self._list_pipelines,
            "trigger_pipeline": self._trigger_pipeline,
            "create_merge_request": self._create_merge_request,
            "list_merge_requests": self._list_merge_requests,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(None)}
        return handler(params)

    def xǁGitlabConnectorǁexecute__mutmut_22(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector GitLab.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "list_projects": self._list_projects,
            "get_project": self._get_project,
            "list_pipelines": self._list_pipelines,
            "trigger_pipeline": self._trigger_pipeline,
            "create_merge_request": self._create_merge_request,
            "list_merge_requests": self._list_merge_requests,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(None)

    @_mutmut_mutated(mutants_xǁGitlabConnectorǁvalidate__mutmut)
    def validate(self) -> bool:
        """Valida que el Private Token de GitLab este configurado."""
        if not self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁGitlabConnectorǁvalidate__mutmut_orig(self) -> bool:
        """Valida que el Private Token de GitLab este configurado."""
        if not self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁGitlabConnectorǁvalidate__mutmut_1(self) -> bool:
        """Valida que el Private Token de GitLab este configurado."""
        if self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁGitlabConnectorǁvalidate__mutmut_2(self) -> bool:
        """Valida que el Private Token de GitLab este configurado."""
        if not self._auth_provider:
            return True
        return self._auth_provider.validate()

    @_mutmut_mutated(mutants_xǁGitlabConnectorǁdisconnect__mutmut)
    def disconnect(self) -> bool:
        """Cierra la conexion con GitLab."""
        self._http = None
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁGitlabConnectorǁdisconnect__mutmut_orig(self) -> bool:
        """Cierra la conexion con GitLab."""
        self._http = None
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁGitlabConnectorǁdisconnect__mutmut_1(self) -> bool:
        """Cierra la conexion con GitLab."""
        self._http = ""
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁGitlabConnectorǁdisconnect__mutmut_2(self) -> bool:
        """Cierra la conexion con GitLab."""
        self._http = None
        self._connected = None
        self._log_operation("disconnect")
        return True

    def xǁGitlabConnectorǁdisconnect__mutmut_3(self) -> bool:
        """Cierra la conexion con GitLab."""
        self._http = None
        self._connected = True
        self._log_operation("disconnect")
        return True

    def xǁGitlabConnectorǁdisconnect__mutmut_4(self) -> bool:
        """Cierra la conexion con GitLab."""
        self._http = None
        self._connected = False
        self._log_operation(None)
        return True

    def xǁGitlabConnectorǁdisconnect__mutmut_5(self) -> bool:
        """Cierra la conexion con GitLab."""
        self._http = None
        self._connected = False
        self._log_operation("XXdisconnectXX")
        return True

    def xǁGitlabConnectorǁdisconnect__mutmut_6(self) -> bool:
        """Cierra la conexion con GitLab."""
        self._http = None
        self._connected = False
        self._log_operation("DISCONNECT")
        return True

    def xǁGitlabConnectorǁdisconnect__mutmut_7(self) -> bool:
        """Cierra la conexion con GitLab."""
        self._http = None
        self._connected = False
        self._log_operation("disconnect")
        return False

    @_mutmut_mutated(mutants_xǁGitlabConnectorǁ_list_projects__mutmut)
    def _list_projects(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation(None, f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", None)

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation(f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", )

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("XXlist_projectsXX", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("LIST_PROJECTS", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get(None, 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', None)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get(20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', )}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('XXper_pageXX', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('PER_PAGE', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 21)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"XXsuccessXX": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"SUCCESS": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": True, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "XXerrorXX": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "ERROR": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "XXConnector not connectedXX"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "CONNECTOR NOT CONNECTED"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = None
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("XXmembershipXX", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("MEMBERSHIP", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "XXownedXX", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "OWNED", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "XXstarredXX", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "STARRED", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "XXsearchXX", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "SEARCH", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "XXsimpleXX",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "SIMPLE",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "XXarchivedXX", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "ARCHIVED", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "XXvisibilityXX", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "VISIBILITY", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "XXorder_byXX", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "ORDER_BY", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "XXsortXX", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "SORT", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "XXper_pageXX", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "PER_PAGE", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "XXpageXX"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "PAGE"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key not in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = None

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = None
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = None
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get(None, 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", None)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get(1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", )
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("XXpageXX", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("PAGE", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 2)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = None
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get(None, 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", None)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get(20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", )
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("XXper_pageXX", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("PER_PAGE", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 21)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = None

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get(None, 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", None)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get(20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", )

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("XXmax_pagesXX", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("MAX_PAGES", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 21)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(None, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, None):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, ):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page - max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = None
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["XXpageXX"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["PAGE"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = None
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["XXper_pageXX"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["PER_PAGE"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = None

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(None, params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=None)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", )

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("XX/projectsXX", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/PROJECTS", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "XXsuccessXX": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "SUCCESS": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": True,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "XXerrorXX": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "ERROR": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "XXdetailsXX": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "DETAILS": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = None
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) and len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) != 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 1:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    return

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(None)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = None
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get(None, "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", None)
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", )
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("XXX-Next-PageXX", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("x-next-page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-NEXT-PAGE", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "XXXX")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    return

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = None  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get(None, str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", None)  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get(str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", )  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("XXX-TotalXX", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("x-total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-TOTAL", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(None))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "XXsuccessXX": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "SUCCESS": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": False,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "XXprojectsXX": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "PROJECTS": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "XXtotal_countXX": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "TOTAL_COUNT": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(None) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_134(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(None)
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_135(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"XXsuccessXX": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_136(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"SUCCESS": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_137(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": True, "error": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_138(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "XXerrorXX": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_139(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "ERROR": str(e)}

    def xǁGitlabConnectorǁ_list_projects__mutmut_140(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista proyectos de GitLab.

        Args:
            params: Opcionalmente 'membership', 'search', 'per_page', 'page',
                    'owned', 'starred', 'simple', 'archived', 'visibility'
        """
        self._log_operation("list_projects", f"per_page={params.get('per_page', 20)}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        query_params: dict[str, Any] = {}
        for key in ("membership", "owned", "starred", "search", "simple",
                     "archived", "visibility", "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_projects: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 20)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get("/projects", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                projects = resp.json()
                if not isinstance(projects, list) or len(projects) == 0:
                    break

                all_projects.extend(projects)

                # GitLab uses X-Next-Page header for pagination
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            # Get total count from header
            total = resp.headers.get("X-Total", str(len(all_projects)))  # type: ignore[possibly-undefined]

            return {
                "success": True,
                "projects": all_projects,
                "total_count": int(total) if total.isdigit() else len(all_projects),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_projects error: {e}")
            return {"success": False, "error": str(None)}

    @_mutmut_mutated(mutants_xǁGitlabConnectorǁ_get_project__mutmut)
    def _get_project(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = None
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get(None, "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", None)
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", )
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("XXproject_idXX", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("PROJECT_ID", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "XXXX")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"XXsuccessXX": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"SUCCESS": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": True, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "XXerrorXX": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "ERROR": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "XXParametro requerido: project_idXX"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "PARAMETRO REQUERIDO: PROJECT_ID"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation(None, f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", None)

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation(f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", )

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("XXget_projectXX", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("GET_PROJECT", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"XXsuccessXX": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"SUCCESS": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": True, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "XXerrorXX": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "ERROR": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "XXConnector not connectedXX"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "CONNECTOR NOT CONNECTED"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = None

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(None, safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe=None)

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), )

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(None), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="XXXX")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = None
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(None)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "XXsuccessXX": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "SUCCESS": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": True,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "XXerrorXX": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "ERROR": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "XXdetailsXX": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "DETAILS": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = None
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "XXsuccessXX": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "SUCCESS": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": False,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "XXidXX": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "ID": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get(None),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("XXidXX"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("ID"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "XXnameXX": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "NAME": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get(None, ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", None),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get(""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("XXnameXX", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("NAME", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", "XXXX"),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "XXpath_with_namespaceXX": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "PATH_WITH_NAMESPACE": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get(None, ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", None),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get(""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("XXpath_with_namespaceXX", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("PATH_WITH_NAMESPACE", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", "XXXX"),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "XXweb_urlXX": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "WEB_URL": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get(None, ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", None),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get(""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("XXweb_urlXX", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("WEB_URL", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", "XXXX"),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "XXdescriptionXX": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "DESCRIPTION": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get(None, ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", None),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get(""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("XXdescriptionXX", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("DESCRIPTION", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", "XXXX"),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "XXdefault_branchXX": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "DEFAULT_BRANCH": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get(None, ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", None),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get(""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("XXdefault_branchXX", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("DEFAULT_BRANCH", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", "XXXX"),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "XXvisibilityXX": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "VISIBILITY": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get(None, ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", None),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get(""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("XXvisibilityXX", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("VISIBILITY", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", "XXXX"),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "XXcreated_atXX": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "CREATED_AT": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get(None, ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", None),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get(""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("XXcreated_atXX", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("CREATED_AT", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", "XXXX"),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "XXlast_activity_atXX": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "LAST_ACTIVITY_AT": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get(None, ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", None),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get(""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("XXlast_activity_atXX", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("LAST_ACTIVITY_AT", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", "XXXX"),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"XXsuccessXX": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"SUCCESS": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": False, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_134(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "XXdataXX": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_135(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "DATA": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_136(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(None)
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_137(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"XXsuccessXX": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_138(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"SUCCESS": False, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_139(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": True, "error": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_140(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "XXerrorXX": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_141(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "ERROR": str(e)}

    def xǁGitlabConnectorǁ_get_project__mutmut_142(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene un proyecto por su ID.

        Args:
            params: Debe contener 'project_id'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("get_project", f"id={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        # URL-encode the project ID (needed for NAMESPACE/PROJECT format)
        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        try:
            resp = self._http.get(f"/projects/{encoded_id}")
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "name": data.get("name", ""),
                    "path_with_namespace": data.get("path_with_namespace", ""),
                    "web_url": data.get("web_url", ""),
                    "description": data.get("description", ""),
                    "default_branch": data.get("default_branch", ""),
                    "visibility": data.get("visibility", ""),
                    "created_at": data.get("created_at", ""),
                    "last_activity_at": data.get("last_activity_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector get_project error: {e}")
            return {"success": False, "error": str(None)}

    @_mutmut_mutated(mutants_xǁGitlabConnectorǁ_list_pipelines__mutmut)
    def _list_pipelines(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = None
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get(None, "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", None)
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", )
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("XXproject_idXX", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("PROJECT_ID", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "XXXX")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"XXsuccessXX": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"SUCCESS": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": True, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "XXerrorXX": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "ERROR": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "XXParametro requerido: project_idXX"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "PARAMETRO REQUERIDO: PROJECT_ID"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation(None, f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", None)

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation(f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", )

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("XXlist_pipelinesXX", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("LIST_PIPELINES", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"XXsuccessXX": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"SUCCESS": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": True, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "XXerrorXX": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "ERROR": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "XXConnector not connectedXX"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "CONNECTOR NOT CONNECTED"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = None

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(None, safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe=None)

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), )

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(None), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="XXXX")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = None
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("XXstatusXX", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("STATUS", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "XXrefXX", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "REF", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "XXshaXX", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "SHA", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "XXyaml_errorsXX", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "YAML_ERRORS", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "XXnameXX", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "NAME", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "XXusernameXX",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "USERNAME",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "XXorder_byXX", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "ORDER_BY", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "XXsortXX", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "SORT", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "XXper_pageXX", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "PER_PAGE", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "XXpageXX"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "PAGE"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key not in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = None

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = None
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = None
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get(None, 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", None)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get(1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", )
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("XXpageXX", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("PAGE", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 2)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = None
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get(None, 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", None)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get(20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", )
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("XXper_pageXX", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("PER_PAGE", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 21)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = None

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get(None, 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", None)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get(10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", )

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("XXmax_pagesXX", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("MAX_PAGES", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 11)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(None, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, None):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, ):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page - max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = None
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["XXpageXX"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["PAGE"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = None
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["XXper_pageXX"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["PER_PAGE"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = None

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(None, params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=None)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", )

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "XXsuccessXX": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "SUCCESS": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": True,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "XXerrorXX": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "ERROR": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "XXdetailsXX": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "DETAILS": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = None
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) and len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) != 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 1:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    return

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(None)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = None
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get(None, "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", None)
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", )
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("XXX-Next-PageXX", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("x-next-page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-NEXT-PAGE", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "XXXX")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    return

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "XXsuccessXX": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "SUCCESS": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": False,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "XXpipelinesXX": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_134(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "PIPELINES": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_135(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "XXtotal_countXX": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_136(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "TOTAL_COUNT": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_137(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(None)
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_138(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"XXsuccessXX": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_139(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"SUCCESS": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_140(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": True, "error": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_141(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "XXerrorXX": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_142(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "ERROR": str(e)}

    def xǁGitlabConnectorǁ_list_pipelines__mutmut_143(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista pipelines de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'status', 'per_page', 'page',
                    'ref', 'sha', 'yaml_errors', 'name', 'username', 'order_by', 'sort'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_pipelines", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("status", "ref", "sha", "yaml_errors", "name", "username",
                     "order_by", "sort", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_pipelines: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/pipelines", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                pipelines = resp.json()
                if not isinstance(pipelines, list) or len(pipelines) == 0:
                    break

                all_pipelines.extend(pipelines)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "pipelines": all_pipelines,
                "total_count": len(all_pipelines),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_pipelines error: {e}")
            return {"success": False, "error": str(None)}

    @_mutmut_mutated(mutants_xǁGitlabConnectorǁ_trigger_pipeline__mutmut)
    def _trigger_pipeline(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = None
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get(None, "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", None)
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", )
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("XXproject_idXX", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("PROJECT_ID", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "XXXX")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = None
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get(None, "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", None)
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", )
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("XXrefXX", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("REF", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "XXmainXX")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "MAIN")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"XXsuccessXX": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"SUCCESS": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": True, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "XXerrorXX": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "ERROR": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "XXParametro requerido: project_idXX"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "PARAMETRO REQUERIDO: PROJECT_ID"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation(None, f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", None)

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation(f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", )

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("XXtrigger_pipelineXX", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("TRIGGER_PIPELINE", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"XXsuccessXX": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"SUCCESS": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": True, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "XXerrorXX": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "ERROR": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "XXConnector not connectedXX"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "CONNECTOR NOT CONNECTED"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = None

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(None, safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe=None)

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), )

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(None), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="XXXX")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = None
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"XXrefXX": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"REF": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "XXvariablesXX" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "VARIABLES" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" not in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = None
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["XXvariablesXX"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["VARIABLES"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = None
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["XXvariablesXX"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["VARIABLES"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"XXkeyXX": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"KEY": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "XXvalueXX": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "VALUE": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = None

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["XXvariablesXX"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["VARIABLES"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = None
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(None, json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=None)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", )
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "XXsuccessXX": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "SUCCESS": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": True,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "XXerrorXX": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "ERROR": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "XXdetailsXX": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "DETAILS": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = None
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "XXsuccessXX": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "SUCCESS": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": False,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "XXidXX": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "ID": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get(None),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("XXidXX"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("ID"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "XXstatusXX": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "STATUS": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get(None, "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", None),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", ),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("XXstatusXX", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("STATUS", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "XXpendingXX"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "PENDING"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "XXrefXX": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "REF": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get(None, ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", None),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get(ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("XXrefXX", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("REF", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "XXshaXX": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "SHA": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get(None, ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", None),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get(""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("XXshaXX", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("SHA", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", "XXXX"),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "XXweb_urlXX": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "WEB_URL": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get(None, ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", None),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get(""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("XXweb_urlXX", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("WEB_URL", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", "XXXX"),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "XXcreated_atXX": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "CREATED_AT": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get(None, ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", None),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get(""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("XXcreated_atXX", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("CREATED_AT", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_134(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", "XXXX"),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_135(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"XXsuccessXX": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_136(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"SUCCESS": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_137(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": False, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_138(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "XXdataXX": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_139(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "DATA": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_140(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(None)
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_141(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"XXsuccessXX": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_142(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"SUCCESS": False, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_143(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": True, "error": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_144(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "XXerrorXX": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_145(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "ERROR": str(e)}

    def xǁGitlabConnectorǁ_trigger_pipeline__mutmut_146(self, params: dict[str, Any]) -> dict[str, Any]:
        """Dispara un pipeline en un proyecto.

        Args:
            params: Debe contener 'project_id' y 'ref', opcionalmente 'variables'
        """
        project_id = params.get("project_id", "")
        ref = params.get("ref", "main")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("trigger_pipeline", f"project={project_id}, ref={ref}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {"ref": ref}
        if "variables" in params:
            # GitLab expects variables as [{"key": "VAR", "value": "val"}]
            variables = params["variables"]
            if isinstance(variables, dict):
                body["variables"] = [
                    {"key": k, "value": v} for k, v in variables.items()
                ]
            elif isinstance(variables, list):
                body["variables"] = variables

        try:
            resp = self._http.post(f"/projects/{encoded_id}/pipeline", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "id": data.get("id"),
                    "status": data.get("status", "pending"),
                    "ref": data.get("ref", ref),
                    "sha": data.get("sha", ""),
                    "web_url": data.get("web_url", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector trigger_pipeline error: {e}")
            return {"success": False, "error": str(None)}

    @_mutmut_mutated(mutants_xǁGitlabConnectorǁ_create_merge_request__mutmut)
    def _create_merge_request(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = None
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get(None, "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", None)
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", )
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("XXproject_idXX", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("PROJECT_ID", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "XXXX")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = None
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get(None, "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", None)
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", )
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("XXsource_branchXX", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("SOURCE_BRANCH", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "XXXX")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = None
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get(None, "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", None)
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", )
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("XXtitleXX", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("TITLE", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "XXXX")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = None
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get(None, "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", None)
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", )
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("XXtarget_branchXX", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("TARGET_BRANCH", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "XXmainXX")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "MAIN")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch and not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id and not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"XXsuccessXX": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"SUCCESS": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": True, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "XXerrorXX": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "ERROR": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "XXParametros requeridos: project_id, source_branch, titleXX"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "PARAMETROS REQUERIDOS: PROJECT_ID, SOURCE_BRANCH, TITLE"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation(None, f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", None)

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation(f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", )

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("XXcreate_merge_requestXX", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("CREATE_MERGE_REQUEST", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"XXsuccessXX": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"SUCCESS": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": True, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "XXerrorXX": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "ERROR": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "XXConnector not connectedXX"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "CONNECTOR NOT CONNECTED"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = None

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(None, safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe=None)

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), )

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(None), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="XXXX")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = None
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "XXsource_branchXX": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "SOURCE_BRANCH": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "XXtarget_branchXX": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "TARGET_BRANCH": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "XXtitleXX": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "TITLE": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("XXdescriptionXX", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("DESCRIPTION", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "XXassignee_idsXX", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "ASSIGNEE_IDS", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "XXreviewer_idsXX", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "REVIEWER_IDS", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "XXlabelsXX", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "LABELS", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "XXmilestone_idXX", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "MILESTONE_ID", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "XXremove_source_branchXX", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "REMOVE_SOURCE_BRANCH", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "XXsquashXX"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "SQUASH"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key not in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = None

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = None
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(None, json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=None)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", )
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "XXsuccessXX": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "SUCCESS": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": True,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "XXerrorXX": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "ERROR": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "XXdetailsXX": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "DETAILS": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = None
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "XXsuccessXX": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "SUCCESS": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": False,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "XXiidXX": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "IID": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get(None),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("XXiidXX"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("IID"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "XXtitleXX": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "TITLE": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get(None, title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", None),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get(title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", ),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("XXtitleXX", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("TITLE", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "XXstateXX": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "STATE": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get(None, "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", None),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", ),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("XXstateXX", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("STATE", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "XXopenedXX"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "OPENED"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "XXweb_urlXX": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "WEB_URL": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_134(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get(None, ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_135(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", None),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_136(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get(""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_137(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_138(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("XXweb_urlXX", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_139(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("WEB_URL", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_140(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", "XXXX"),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_141(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "XXsource_branchXX": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_142(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "SOURCE_BRANCH": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_143(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get(None, source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_144(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", None),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_145(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get(source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_146(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", ),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_147(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("XXsource_branchXX", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_148(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("SOURCE_BRANCH", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_149(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "XXtarget_branchXX": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_150(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "TARGET_BRANCH": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_151(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get(None, target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_152(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", None),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_153(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get(target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_154(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", ),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_155(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("XXtarget_branchXX", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_156(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("TARGET_BRANCH", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_157(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "XXauthorXX": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_158(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "AUTHOR": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_159(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get(None, ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_160(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", None),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_161(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get(""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_162(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_163(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get(None, {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_164(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", None).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_165(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get({}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_166(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", ).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_167(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("XXauthorXX", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_168(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("AUTHOR", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_169(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("XXusernameXX", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_170(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("USERNAME", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_171(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", "XXXX"),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_172(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "XXcreated_atXX": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_173(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "CREATED_AT": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_174(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get(None, ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_175(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", None),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_176(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get(""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_177(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_178(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("XXcreated_atXX", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_179(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("CREATED_AT", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_180(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", "XXXX"),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_181(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"XXsuccessXX": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_182(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"SUCCESS": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_183(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": False, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_184(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "XXdataXX": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_185(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "DATA": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_186(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(None)
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_187(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"XXsuccessXX": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_188(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"SUCCESS": False, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_189(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": True, "error": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_190(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "XXerrorXX": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_191(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "ERROR": str(e)}

    def xǁGitlabConnectorǁ_create_merge_request__mutmut_192(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea un merge request en un proyecto.

        Args:
            params: Debe contener 'project_id', 'source_branch', 'target_branch', 'title'
        """
        project_id = params.get("project_id", "")
        source_branch = params.get("source_branch", "")
        title = params.get("title", "")
        target_branch = params.get("target_branch", "main")
        if not project_id or not source_branch or not title:
            return {"success": False, "error": "Parametros requeridos: project_id, source_branch, title"}
        self._log_operation("create_merge_request", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        body: dict[str, Any] = {
            "source_branch": source_branch,
            "target_branch": target_branch,
            "title": title,
        }
        for key in ("description", "assignee_ids", "reviewer_ids", "labels", "milestone_id", "remove_source_branch", "squash"):
            if key in params:
                body[key] = params[key]

        try:
            resp = self._http.post(f"/projects/{encoded_id}/merge_requests", json=body)
            if not resp.ok:
                return {
                    "success": False,
                    "error": f"GitLab API error: {resp.status_code}",
                    "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                }

            data = resp.json()
            if isinstance(data, dict):
                return {
                    "success": True,
                    "iid": data.get("iid"),
                    "title": data.get("title", title),
                    "state": data.get("state", "opened"),
                    "web_url": data.get("web_url", ""),
                    "source_branch": data.get("source_branch", source_branch),
                    "target_branch": data.get("target_branch", target_branch),
                    "author": data.get("author", {}).get("username", ""),
                    "created_at": data.get("created_at", ""),
                }
            return {"success": True, "data": data}

        except HTTPClientError as e:
            logger.error(f"GitlabConnector create_merge_request error: {e}")
            return {"success": False, "error": str(None)}

    @_mutmut_mutated(mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut)
    def _list_merge_requests(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = None
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get(None, "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", None)
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", )
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("XXproject_idXX", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("PROJECT_ID", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "XXXX")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"XXsuccessXX": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"SUCCESS": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": True, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "XXerrorXX": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "ERROR": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "XXParametro requerido: project_idXX"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "PARAMETRO REQUERIDO: PROJECT_ID"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation(None, f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", None)

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation(f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", )

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("XXlist_merge_requestsXX", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("LIST_MERGE_REQUESTS", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"XXsuccessXX": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"SUCCESS": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": True, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "XXerrorXX": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "ERROR": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "XXConnector not connectedXX"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "CONNECTOR NOT CONNECTED"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = None

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(None, safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe=None)

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), )

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(None), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="XXXX")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = None
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("XXstateXX", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("STATE", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "XXorder_byXX", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "ORDER_BY", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "XXsortXX", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "SORT", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "XXmilestoneXX", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "MILESTONE", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "XXlabelsXX",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "LABELS",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "XXauthor_idXX", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "AUTHOR_ID", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "XXassignee_idXX", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "ASSIGNEE_ID", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "XXsearchXX", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "SEARCH", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "XXsource_branchXX",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "SOURCE_BRANCH",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "XXtarget_branchXX", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "TARGET_BRANCH", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "XXper_pageXX", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "PER_PAGE", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "XXpageXX"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "PAGE"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key not in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = None

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = None
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = None
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get(None, 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", None)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get(1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", )
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("XXpageXX", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("PAGE", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 2)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = None
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get(None, 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", None)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get(20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", )
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("XXper_pageXX", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("PER_PAGE", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 21)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = None

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get(None, 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", None)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get(10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", )

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("XXmax_pagesXX", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("MAX_PAGES", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 11)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(None, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, None):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, ):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page - max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = None
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["XXpageXX"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["PAGE"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = None
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["XXper_pageXX"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["PER_PAGE"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = None

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(None, params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=None)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", )

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "XXsuccessXX": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "SUCCESS": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": True,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "XXerrorXX": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "ERROR": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "XXdetailsXX": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "DETAILS": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = None
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) and len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) != 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 1:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    return

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(None)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = None
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get(None, "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", None)
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", )
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("XXX-Next-PageXX", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("x-next-page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-NEXT-PAGE", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "XXXX")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    return

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_134(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "XXsuccessXX": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_135(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "SUCCESS": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_136(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": False,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_137(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "XXmerge_requestsXX": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_138(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "MERGE_REQUESTS": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_139(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "XXtotal_countXX": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_140(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "TOTAL_COUNT": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_141(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(None)
            return {"success": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_142(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"XXsuccessXX": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_143(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"SUCCESS": False, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_144(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": True, "error": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_145(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "XXerrorXX": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_146(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "ERROR": str(e)}

    def xǁGitlabConnectorǁ_list_merge_requests__mutmut_147(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista merge requests de un proyecto.

        Args:
            params: Debe contener 'project_id' y opcionalmente 'state', 'per_page', 'page',
                    'order_by', 'sort', 'milestone', 'labels', 'author_id', 'assignee_id',
                    'search', 'source_branch', 'target_branch'
        """
        project_id = params.get("project_id", "")
        if not project_id:
            return {"success": False, "error": "Parametro requerido: project_id"}
        self._log_operation("list_merge_requests", f"project={project_id}")

        if not self._http:
            return {"success": False, "error": "Connector not connected"}

        import urllib.parse
        encoded_id = urllib.parse.quote(str(project_id), safe="")

        query_params: dict[str, Any] = {}
        for key in ("state", "order_by", "sort", "milestone", "labels",
                     "author_id", "assignee_id", "search", "source_branch",
                     "target_branch", "per_page", "page"):
            if key in params:
                query_params[key] = params[key]

        try:
            all_mrs: list[dict[str, Any]] = []
            page = query_params.get("page", 1)
            per_page = query_params.get("per_page", 20)
            max_pages = params.get("max_pages", 10)

            for current_page in range(page, page + max_pages):
                query_params["page"] = current_page
                query_params["per_page"] = per_page
                resp = self._http.get(f"/projects/{encoded_id}/merge_requests", params=query_params)

                if not resp.ok:
                    return {
                        "success": False,
                        "error": f"GitLab API error: {resp.status_code}",
                        "details": resp.body if isinstance(resp.body, (dict, str)) else None,
                    }

                mrs = resp.json()
                if not isinstance(mrs, list) or len(mrs) == 0:
                    break

                all_mrs.extend(mrs)

                # GitLab pagination header
                next_page = resp.headers.get("X-Next-Page", "")
                if not next_page:
                    break

            return {
                "success": True,
                "merge_requests": all_mrs,
                "total_count": len(all_mrs),
            }

        except HTTPClientError as e:
            logger.error(f"GitlabConnector list_merge_requests error: {e}")
            return {"success": False, "error": str(None)}

mutants_xǁGitlabConnectorǁ__init____mutmut['_mutmut_orig'] = GitlabConnector.xǁGitlabConnectorǁ__init____mutmut_orig # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ__init____mutmut['xǁGitlabConnectorǁ__init____mutmut_1'] = GitlabConnector.xǁGitlabConnectorǁ__init____mutmut_1 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ__init____mutmut['xǁGitlabConnectorǁ__init____mutmut_2'] = GitlabConnector.xǁGitlabConnectorǁ__init____mutmut_2 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ__init____mutmut['xǁGitlabConnectorǁ__init____mutmut_3'] = GitlabConnector.xǁGitlabConnectorǁ__init____mutmut_3 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ__init____mutmut['xǁGitlabConnectorǁ__init____mutmut_4'] = GitlabConnector.xǁGitlabConnectorǁ__init____mutmut_4 # type: ignore # mutmut generated

mutants_xǁGitlabConnectorǁ_get_token__mutmut['_mutmut_orig'] = GitlabConnector.xǁGitlabConnectorǁ_get_token__mutmut_orig # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_get_token__mutmut['xǁGitlabConnectorǁ_get_token__mutmut_1'] = GitlabConnector.xǁGitlabConnectorǁ_get_token__mutmut_1 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_get_token__mutmut['xǁGitlabConnectorǁ_get_token__mutmut_2'] = GitlabConnector.xǁGitlabConnectorǁ_get_token__mutmut_2 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_get_token__mutmut['xǁGitlabConnectorǁ_get_token__mutmut_3'] = GitlabConnector.xǁGitlabConnectorǁ_get_token__mutmut_3 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_get_token__mutmut['xǁGitlabConnectorǁ_get_token__mutmut_4'] = GitlabConnector.xǁGitlabConnectorǁ_get_token__mutmut_4 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_get_token__mutmut['xǁGitlabConnectorǁ_get_token__mutmut_5'] = GitlabConnector.xǁGitlabConnectorǁ_get_token__mutmut_5 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_get
# type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_20'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_20 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_21'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_21 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_22'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_22 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_23'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_23 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_24'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_24 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_25'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_25 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_26'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_26 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_27'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_27 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_28'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_28 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_29'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_29 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_30'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_30 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_31'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_31 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_32'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_32 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_33'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_33 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_34'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_34 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_35'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_35 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_36'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_36 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_37'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_37 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_38'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_38 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_39'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_39 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_40'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_40 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_41'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_41 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_42'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_42 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_43'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_43 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_44'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_44 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_45'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_45 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_46'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_46 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_47'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_47 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_48'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_48 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_49'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_49 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_50'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_50 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_51'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_51 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_52'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_52 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_53'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_53 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_54'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_54 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_55'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_55 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_56'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_56 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_57'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_57 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_58'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_58 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_59'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_59 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_60'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_60 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_61'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_61 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_62'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_62 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_63'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_63 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_64'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_64 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_65'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_65 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_66'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_66 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_67'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_67 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_68'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_68 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_69'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_69 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_70'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_70 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_71'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_71 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_72'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_72 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_73'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_73 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_74'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_74 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_75'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_75 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_76'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_76 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_77'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_77 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_78'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_78 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_79'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_79 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_80'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_80 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_81'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_81 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_82'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_82 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_83'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_83 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_84'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_84 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_85'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_85 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_86'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_86 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_87'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_87 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_88'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_88 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_89'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_89 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_90'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_90 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_91'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_91 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_92'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_92 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_93'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_93 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_94'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_94 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_95'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_95 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_96'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_96 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_97'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_97 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_98'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_98 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_99'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_99 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_100'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_100 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_101'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_101 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_102'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_102 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_103'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_103 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_104'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_104 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_105'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_105 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_106'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_106 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_107'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_107 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_108'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_108 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_109'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_109 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_110'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_110 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_111'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_111 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_112'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_112 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_113'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_113 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_114'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_114 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_115'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_115 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_116'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_116 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_117'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_117 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_118'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_118 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_119'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_119 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_120'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_120 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_121'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_121 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_122'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_122 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_123'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_123 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_124'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_124 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_125'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_125 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_126'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_126 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_127'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_127 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_128'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_128 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_129'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_129 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_130'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_130 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_131'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_131 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_132'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_132 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_133'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_133 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_134'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_134 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_135'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_135 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_136'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_136 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_137'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_137 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_138'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_138 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_139'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_139 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_140'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_140 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_141'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_141 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_142'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_142 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_143'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_143 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_144'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_144 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_145'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_145 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_146'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_146 # type: ignore # mutmut generated
mutants_xǁGitlabConnectorǁ_list_merge_requests__mutmut['xǁGitlabConnectorǁ_list_merge_requests__mutmut_147'] = GitlabConnector.xǁGitlabConnectorǁ_list_merge_requests__mutmut_147 # type: ignore # mutmut generated


GITLAB_SCHEMA = ConnectorSchema(
    name="gitlab",
    version="1.0.0",
    description="Gestiona proyectos, pipelines y merge requests via GitLab API",
    category="devops_monitoring",
    icon="git-merge",
    author="Zenic-Flijo",
    actions=[
        ActionDefinition(name="list_projects", description="Lista proyectos", category="read"),
        ActionDefinition(name="get_project", description="Obtiene un proyecto", category="read"),
        ActionDefinition(name="list_pipelines", description="Lista pipelines", category="read"),
        ActionDefinition(name="trigger_pipeline", description="Dispara un pipeline", category="write"),
        ActionDefinition(name="create_merge_request", description="Crea un MR", category="write"),
        ActionDefinition(name="list_merge_requests", description="Lista MRs", category="read"),
    ],
    auth_requirements=[
        AuthRequirement(auth_type="api_key", required_fields=["private_token"], description="GitLab Private Token")
    ],
)
