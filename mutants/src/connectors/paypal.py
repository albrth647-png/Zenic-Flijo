"""
Conector PayPal — Pagos y Transacciones via PayPal API
=========================================================

Permite crear, capturar y reembolsar pagos, gestionar
ordenes y consultar transacciones via la API de PayPal.
"""

from __future__ import annotations

from typing import Any

from src.core.logging import setup_logging
from src.sdk.base import BaseConnector
from src.sdk.http_client import HttpClient, HTTPClientError
from src.sdk.schema import ActionDefinition, AuthRequirement, ConnectorSchema

logger = setup_logging(__name__)


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_xǁPaypalConnectorǁ__init____mutmut: MutantDict = {}  # type: ignore
mutants_xǁPaypalConnectorǁconnect__mutmut: MutantDict = {}  # type: ignore
mutants_xǁPaypalConnectorǁexecute__mutmut: MutantDict = {}  # type: ignore
mutants_xǁPaypalConnectorǁvalidate__mutmut: MutantDict = {}  # type: ignore
mutants_xǁPaypalConnectorǁdisconnect__mutmut: MutantDict = {}  # type: ignore
mutants_xǁPaypalConnectorǁ_create_order__mutmut: MutantDict = {}  # type: ignore
mutants_xǁPaypalConnectorǁ_capture_order__mutmut: MutantDict = {}  # type: ignore
mutants_xǁPaypalConnectorǁ_get_order__mutmut: MutantDict = {}  # type: ignore
mutants_xǁPaypalConnectorǁ_refund_payment__mutmut: MutantDict = {}  # type: ignore
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut: MutantDict = {}  # type: ignore
mutants_xǁPaypalConnectorǁ_get_payment_details__mutmut: MutantDict = {}  # type: ignore


class PaypalConnector(BaseConnector):
    """Conector para PayPal: pagos, ordenes y transacciones."""

    name = "paypal"
    version = "1.0.0"
    description = "Crea y gestiona pagos, ordenes y transacciones via PayPal"
    category = "finance_payments"
    icon = "credit-card"
    author = "Zenic-Flijo"

    @_mutmut_mutated(mutants_xǁPaypalConnectorǁ__init____mutmut)
    def __init__(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://api-m.paypal.com"
        self._sandbox_url: str = "https://api-m.sandbox.paypal.com"
        self._http: HttpClient | None = None

    def xǁPaypalConnectorǁ__init____mutmut_orig(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://api-m.paypal.com"
        self._sandbox_url: str = "https://api-m.sandbox.paypal.com"
        self._http: HttpClient | None = None

    def xǁPaypalConnectorǁ__init____mutmut_1(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = None
        self._sandbox_url: str = "https://api-m.sandbox.paypal.com"
        self._http: HttpClient | None = None

    def xǁPaypalConnectorǁ__init____mutmut_2(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "XXhttps://api-m.paypal.comXX"
        self._sandbox_url: str = "https://api-m.sandbox.paypal.com"
        self._http: HttpClient | None = None

    def xǁPaypalConnectorǁ__init____mutmut_3(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "HTTPS://API-M.PAYPAL.COM"
        self._sandbox_url: str = "https://api-m.sandbox.paypal.com"
        self._http: HttpClient | None = None

    def xǁPaypalConnectorǁ__init____mutmut_4(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://api-m.paypal.com"
        self._sandbox_url: str = None
        self._http: HttpClient | None = None

    def xǁPaypalConnectorǁ__init____mutmut_5(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://api-m.paypal.com"
        self._sandbox_url: str = "XXhttps://api-m.sandbox.paypal.comXX"
        self._http: HttpClient | None = None

    def xǁPaypalConnectorǁ__init____mutmut_6(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://api-m.paypal.com"
        self._sandbox_url: str = "HTTPS://API-M.SANDBOX.PAYPAL.COM"
        self._http: HttpClient | None = None

    def xǁPaypalConnectorǁ__init____mutmut_7(self, **kwargs: Any) -> None:
        super().__init__(**kwargs)
        self._base_url: str = "https://api-m.paypal.com"
        self._sandbox_url: str = "https://api-m.sandbox.paypal.com"
        self._http: HttpClient | None = ""

    @_mutmut_mutated(mutants_xǁPaypalConnectorǁconnect__mutmut)
    def connect(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_orig(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_1(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider and not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_2(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_3(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_4(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error(None)
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_5(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("XXPaypalConnector: credenciales no configuradasXX")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_6(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("paypalconnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_7(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PAYPALCONNECTOR: CREDENCIALES NO CONFIGURADAS")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_8(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return True
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_9(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = None
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_10(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = None
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_11(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get(None, False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_12(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", None)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_13(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get(False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_14(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", )
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_15(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("XXsandboxXX", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_16(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("SANDBOX", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_17(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", True)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_18(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = None

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_19(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = None

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_20(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=None,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_21(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=None,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_22(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_23(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_24(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = None
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_25(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get(None, "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_26(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", None)
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_27(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_28(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", )
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_29(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("XXclient_idXX", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_30(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("CLIENT_ID", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_31(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "XXXX")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_32(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = None
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_33(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get(None, "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_34(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", None)
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_35(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_36(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", )
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_37(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("XXclient_secretXX", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_38(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("CLIENT_SECRET", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_39(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "XXXX")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_40(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = None

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_41(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get(None, "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_42(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", None)

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_43(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_44(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", )

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_45(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("XXaccess_tokenXX", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_46(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("ACCESS_TOKEN", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_47(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "XXXX")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_48(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth(None, token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_49(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=None)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_50(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth(token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_51(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", )
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_52(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("XXBearerXX", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_53(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_54(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("BEARER", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_55(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id or client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_56(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = None
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_57(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    None,
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_58(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data=None,
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_59(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers=None,
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_60(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_61(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_62(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_63(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "XX/v1/oauth2/tokenXX",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_64(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/V1/OAUTH2/TOKEN",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_65(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"XXgrant_typeXX": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_66(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"GRANT_TYPE": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_67(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "XXclient_credentialsXX"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_68(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "CLIENT_CREDENTIALS"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_69(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "XXAcceptXX": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_70(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_71(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "ACCEPT": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_72(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "XXapplication/jsonXX",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_73(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "APPLICATION/JSON",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_74(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "XXContent-TypeXX": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_75(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "content-type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_76(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "CONTENT-TYPE": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_77(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "XXapplication/x-www-form-urlencodedXX",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_78(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "APPLICATION/X-WWW-FORM-URLENCODED",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_79(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = None
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_80(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() and {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_81(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = None
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_82(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get(None, "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_83(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", None)
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_84(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_85(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", )
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_86(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("XXaccess_tokenXX", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_87(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("ACCESS_TOKEN", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_88(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "XXXX")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_89(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth(None, token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_90(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=None)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_91(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth(token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_92(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", )
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_93(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("XXBearerXX", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_94(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_95(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("BEARER", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_96(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth(None, username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_97(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=None, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_98(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=None)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_99(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth(username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_100(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_101(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, )
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_102(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("XXBasicXX", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_103(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_104(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("BASIC", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_105(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(None)

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_106(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = None
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_107(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = False
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_108(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation(None, "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_109(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", None)
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_110(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_111(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", )
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_112(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("XXconnectXX", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_113(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("CONNECT", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_114(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "XXOAuth2 configurado para PayPalXX")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_115(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "oauth2 configurado para paypal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_116(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAUTH2 CONFIGURADO PARA PAYPAL")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_117(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return False
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_118(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(None)
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_119(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = None
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_120(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = None
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_121(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get(None, False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_122(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", None)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_123(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get(False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_124(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", )
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_125(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("XXsandboxXX", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_126(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("SANDBOX", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_127(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", True)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_128(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = None
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_129(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = None
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_130(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=None,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_131(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=None,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_132(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_133(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_134(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = None
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_135(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get(None, "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_136(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", None)
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_137(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_138(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", )
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_139(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("XXaccess_tokenXX", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_140(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("ACCESS_TOKEN", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_141(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "XXXX")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_142(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth(None, token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_143(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=None)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_144(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth(token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_145(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", )
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_146(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("XXBearerXX", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_147(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_148(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("BEARER", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_149(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = None
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_150(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = False
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_151(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation(None, f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_152(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", None)
            return True

    def xǁPaypalConnectorǁconnect__mutmut_153(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation(f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_154(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", )
            return True

    def xǁPaypalConnectorǁconnect__mutmut_155(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("XXconnectXX", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_156(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("CONNECT", f"OAuth2 configurado (token request fallo: {e})")
            return True

    def xǁPaypalConnectorǁconnect__mutmut_157(self) -> bool:
        """Establece conexion con la API de PayPal."""
        if not self._auth_provider or not self._auth_provider.validate():
            logger.error("PaypalConnector: credenciales no configuradas")
            return False
        try:
            creds = self._auth_provider.get_credentials()
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url

            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )

            # PayPal OAuth2: obtain access token using client_id/client_secret
            client_id = creds.get("client_id", "")
            client_secret = creds.get("client_secret", "")
            access_token = creds.get("access_token", "")

            if access_token:
                # Use provided access token directly
                self._http.set_auth("Bearer", token=access_token)
            elif client_id and client_secret:
                # Obtain access token via OAuth2 client credentials flow
                token_resp = self._http.post(
                    "/v1/oauth2/token",
                    data={"grant_type": "client_credentials"},
                    headers={
                        "Accept": "application/json",
                        "Content-Type": "application/x-www-form-urlencoded",
                    },
                )
                if token_resp.ok:
                    token_data = token_resp.json() or {}
                    new_token = token_data.get("access_token", "")
                    if new_token:
                        self._http.set_auth("Bearer", token=new_token)
                else:
                    # Fallback to Basic auth
                    self._http.set_auth("Basic", username=client_id, password=client_secret)
                    logger.warning(f"PaypalConnector: fallo al obtener token OAuth2 - {token_resp.status_code}")

            self._connected = True
            self._log_operation("connect", "OAuth2 configurado para PayPal")
            return True
        except HTTPClientError as e:
            logger.warning(f"PaypalConnector: error durante conexion - {e}")
            # Still set up client with available credentials
            creds = self._auth_provider.get_credentials() if self._auth_provider else {}
            sandbox = creds.get("sandbox", False)
            base_url = self._sandbox_url if sandbox else self._base_url
            self._http = HttpClient(
                base_url=base_url,
                connector_name=self.name,
            )
            access_token = creds.get("access_token", "")
            if access_token:
                self._http.set_auth("Bearer", token=access_token)
            self._connected = True
            self._log_operation("connect", f"OAuth2 configurado (token request fallo: {e})")
            return False

    @_mutmut_mutated(mutants_xǁPaypalConnectorǁexecute__mutmut)
    def execute(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector PayPal.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_order": self._create_order,
            "capture_order": self._capture_order,
            "get_order": self._get_order,
            "refund_payment": self._refund_payment,
            "list_transactions": self._list_transactions,
            "get_payment_details": self._get_payment_details,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁPaypalConnectorǁexecute__mutmut_orig(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector PayPal.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_order": self._create_order,
            "capture_order": self._capture_order,
            "get_order": self._get_order,
            "refund_payment": self._refund_payment,
            "list_transactions": self._list_transactions,
            "get_payment_details": self._get_payment_details,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁPaypalConnectorǁexecute__mutmut_1(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector PayPal.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = None
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁPaypalConnectorǁexecute__mutmut_2(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector PayPal.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "XXcreate_orderXX": self._create_order,
            "capture_order": self._capture_order,
            "get_order": self._get_order,
            "refund_payment": self._refund_payment,
            "list_transactions": self._list_transactions,
            "get_payment_details": self._get_payment_details,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁPaypalConnectorǁexecute__mutmut_3(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector PayPal.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "CREATE_ORDER": self._create_order,
            "capture_order": self._capture_order,
            "get_order": self._get_order,
            "refund_payment": self._refund_payment,
            "list_transactions": self._list_transactions,
            "get_payment_details": self._get_payment_details,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁPaypalConnectorǁexecute__mutmut_4(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector PayPal.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_order": self._create_order,
            "XXcapture_orderXX": self._capture_order,
            "get_order": self._get_order,
            "refund_payment": self._refund_payment,
            "list_transactions": self._list_transactions,
            "get_payment_details": self._get_payment_details,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁPaypalConnectorǁexecute__mutmut_5(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector PayPal.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_order": self._create_order,
            "CAPTURE_ORDER": self._capture_order,
            "get_order": self._get_order,
            "refund_payment": self._refund_payment,
            "list_transactions": self._list_transactions,
            "get_payment_details": self._get_payment_details,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁPaypalConnectorǁexecute__mutmut_6(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector PayPal.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_order": self._create_order,
            "capture_order": self._capture_order,
            "XXget_orderXX": self._get_order,
            "refund_payment": self._refund_payment,
            "list_transactions": self._list_transactions,
            "get_payment_details": self._get_payment_details,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁPaypalConnectorǁexecute__mutmut_7(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector PayPal.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_order": self._create_order,
            "capture_order": self._capture_order,
            "GET_ORDER": self._get_order,
            "refund_payment": self._refund_payment,
            "list_transactions": self._list_transactions,
            "get_payment_details": self._get_payment_details,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁPaypalConnectorǁexecute__mutmut_8(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector PayPal.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_order": self._create_order,
            "capture_order": self._capture_order,
            "get_order": self._get_order,
            "XXrefund_paymentXX": self._refund_payment,
            "list_transactions": self._list_transactions,
            "get_payment_details": self._get_payment_details,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁPaypalConnectorǁexecute__mutmut_9(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector PayPal.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_order": self._create_order,
            "capture_order": self._capture_order,
            "get_order": self._get_order,
            "REFUND_PAYMENT": self._refund_payment,
            "list_transactions": self._list_transactions,
            "get_payment_details": self._get_payment_details,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁPaypalConnectorǁexecute__mutmut_10(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector PayPal.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_order": self._create_order,
            "capture_order": self._capture_order,
            "get_order": self._get_order,
            "refund_payment": self._refund_payment,
            "XXlist_transactionsXX": self._list_transactions,
            "get_payment_details": self._get_payment_details,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁPaypalConnectorǁexecute__mutmut_11(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector PayPal.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_order": self._create_order,
            "capture_order": self._capture_order,
            "get_order": self._get_order,
            "refund_payment": self._refund_payment,
            "LIST_TRANSACTIONS": self._list_transactions,
            "get_payment_details": self._get_payment_details,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁPaypalConnectorǁexecute__mutmut_12(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector PayPal.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_order": self._create_order,
            "capture_order": self._capture_order,
            "get_order": self._get_order,
            "refund_payment": self._refund_payment,
            "list_transactions": self._list_transactions,
            "XXget_payment_detailsXX": self._get_payment_details,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁPaypalConnectorǁexecute__mutmut_13(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector PayPal.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_order": self._create_order,
            "capture_order": self._capture_order,
            "get_order": self._get_order,
            "refund_payment": self._refund_payment,
            "list_transactions": self._list_transactions,
            "GET_PAYMENT_DETAILS": self._get_payment_details,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁPaypalConnectorǁexecute__mutmut_14(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector PayPal.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_order": self._create_order,
            "capture_order": self._capture_order,
            "get_order": self._get_order,
            "refund_payment": self._refund_payment,
            "list_transactions": self._list_transactions,
            "get_payment_details": self._get_payment_details,
        }
        handler = None
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁPaypalConnectorǁexecute__mutmut_15(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector PayPal.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_order": self._create_order,
            "capture_order": self._capture_order,
            "get_order": self._get_order,
            "refund_payment": self._refund_payment,
            "list_transactions": self._list_transactions,
            "get_payment_details": self._get_payment_details,
        }
        handler = action_map.get(None)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁPaypalConnectorǁexecute__mutmut_16(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector PayPal.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_order": self._create_order,
            "capture_order": self._capture_order,
            "get_order": self._get_order,
            "refund_payment": self._refund_payment,
            "list_transactions": self._list_transactions,
            "get_payment_details": self._get_payment_details,
        }
        handler = action_map.get(action)
        if handler is not None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁPaypalConnectorǁexecute__mutmut_17(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector PayPal.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_order": self._create_order,
            "capture_order": self._capture_order,
            "get_order": self._get_order,
            "refund_payment": self._refund_payment,
            "list_transactions": self._list_transactions,
            "get_payment_details": self._get_payment_details,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"XXerrorXX": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁPaypalConnectorǁexecute__mutmut_18(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector PayPal.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_order": self._create_order,
            "capture_order": self._capture_order,
            "get_order": self._get_order,
            "refund_payment": self._refund_payment,
            "list_transactions": self._list_transactions,
            "get_payment_details": self._get_payment_details,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"ERROR": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(params)

    def xǁPaypalConnectorǁexecute__mutmut_19(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector PayPal.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_order": self._create_order,
            "capture_order": self._capture_order,
            "get_order": self._get_order,
            "refund_payment": self._refund_payment,
            "list_transactions": self._list_transactions,
            "get_payment_details": self._get_payment_details,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "XXavailableXX": list(action_map.keys())}
        return handler(params)

    def xǁPaypalConnectorǁexecute__mutmut_20(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector PayPal.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_order": self._create_order,
            "capture_order": self._capture_order,
            "get_order": self._get_order,
            "refund_payment": self._refund_payment,
            "list_transactions": self._list_transactions,
            "get_payment_details": self._get_payment_details,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "AVAILABLE": list(action_map.keys())}
        return handler(params)

    def xǁPaypalConnectorǁexecute__mutmut_21(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector PayPal.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_order": self._create_order,
            "capture_order": self._capture_order,
            "get_order": self._get_order,
            "refund_payment": self._refund_payment,
            "list_transactions": self._list_transactions,
            "get_payment_details": self._get_payment_details,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(None)}
        return handler(params)

    def xǁPaypalConnectorǁexecute__mutmut_22(self, action: str, params: dict[str, Any]) -> Any:
        """Ejecuta una accion del conector PayPal.

        Args:
            action: Nombre de la accion a ejecutar
            params: Parametros de la accion
        """
        action_map: dict[str, Any] = {
            "create_order": self._create_order,
            "capture_order": self._capture_order,
            "get_order": self._get_order,
            "refund_payment": self._refund_payment,
            "list_transactions": self._list_transactions,
            "get_payment_details": self._get_payment_details,
        }
        handler = action_map.get(action)
        if handler is None:
            return {"error": f"Accion '{action}' no soportada", "available": list(action_map.keys())}
        return handler(None)

    @_mutmut_mutated(mutants_xǁPaypalConnectorǁvalidate__mutmut)
    def validate(self) -> bool:
        """Valida que las credenciales de PayPal esten configuradas."""
        if not self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁPaypalConnectorǁvalidate__mutmut_orig(self) -> bool:
        """Valida que las credenciales de PayPal esten configuradas."""
        if not self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁPaypalConnectorǁvalidate__mutmut_1(self) -> bool:
        """Valida que las credenciales de PayPal esten configuradas."""
        if self._auth_provider:
            return False
        return self._auth_provider.validate()

    def xǁPaypalConnectorǁvalidate__mutmut_2(self) -> bool:
        """Valida que las credenciales de PayPal esten configuradas."""
        if not self._auth_provider:
            return True
        return self._auth_provider.validate()

    @_mutmut_mutated(mutants_xǁPaypalConnectorǁdisconnect__mutmut)
    def disconnect(self) -> bool:
        """Cierra la conexion con PayPal."""
        self._http = None
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁPaypalConnectorǁdisconnect__mutmut_orig(self) -> bool:
        """Cierra la conexion con PayPal."""
        self._http = None
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁPaypalConnectorǁdisconnect__mutmut_1(self) -> bool:
        """Cierra la conexion con PayPal."""
        self._http = ""
        self._connected = False
        self._log_operation("disconnect")
        return True

    def xǁPaypalConnectorǁdisconnect__mutmut_2(self) -> bool:
        """Cierra la conexion con PayPal."""
        self._http = None
        self._connected = None
        self._log_operation("disconnect")
        return True

    def xǁPaypalConnectorǁdisconnect__mutmut_3(self) -> bool:
        """Cierra la conexion con PayPal."""
        self._http = None
        self._connected = True
        self._log_operation("disconnect")
        return True

    def xǁPaypalConnectorǁdisconnect__mutmut_4(self) -> bool:
        """Cierra la conexion con PayPal."""
        self._http = None
        self._connected = False
        self._log_operation(None)
        return True

    def xǁPaypalConnectorǁdisconnect__mutmut_5(self) -> bool:
        """Cierra la conexion con PayPal."""
        self._http = None
        self._connected = False
        self._log_operation("XXdisconnectXX")
        return True

    def xǁPaypalConnectorǁdisconnect__mutmut_6(self) -> bool:
        """Cierra la conexion con PayPal."""
        self._http = None
        self._connected = False
        self._log_operation("DISCONNECT")
        return True

    def xǁPaypalConnectorǁdisconnect__mutmut_7(self) -> bool:
        """Cierra la conexion con PayPal."""
        self._http = None
        self._connected = False
        self._log_operation("disconnect")
        return False

    @_mutmut_mutated(mutants_xǁPaypalConnectorǁ_create_order__mutmut)
    def _create_order(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = None
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get(None, "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", None)
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", )
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("XXintentXX", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("INTENT", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "XXCAPTUREXX")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "capture")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = None
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get(None, "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", None)
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", )
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("XXamountXX", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("AMOUNT", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "XXXX")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = None
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get(None, "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", None)
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", )
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("XXcurrencyXX", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("CURRENCY", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "XXUSDXX")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "usd")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"XXsuccessXX": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"SUCCESS": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": True, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "XXerrorXX": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "ERROR": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "XXParametro requerido: amountXX"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "PARAMETRO REQUERIDO: AMOUNT"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation(None, f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", None)

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation(f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", )

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("XXcreate_orderXX", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("CREATE_ORDER", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = None
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "XXintentXX": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "INTENT": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "XXpurchase_unitsXX": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "PURCHASE_UNITS": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "XXamountXX": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "AMOUNT": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "XXcurrency_codeXX": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "CURRENCY_CODE": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "XXvalueXX": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "VALUE": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(None),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get(None):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("XXdescriptionXX"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("DESCRIPTION"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = None
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["XXpurchase_unitsXX"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["PURCHASE_UNITS"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][1]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["XXdescriptionXX"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["DESCRIPTION"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["XXdescriptionXX"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["DESCRIPTION"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get(None):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("XXcustom_idXX"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("CUSTOM_ID"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = None
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["XXpurchase_unitsXX"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["PURCHASE_UNITS"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][1]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["XXcustom_idXX"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["CUSTOM_ID"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["XXcustom_idXX"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["CUSTOM_ID"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") and params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get(None) or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("XXreturn_urlXX") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("RETURN_URL") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get(None):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("XXcancel_urlXX"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("CANCEL_URL"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = None
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get(None):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("XXreturn_urlXX"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("RETURN_URL"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = None
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["XXreturn_urlXX"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["RETURN_URL"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["XXreturn_urlXX"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["RETURN_URL"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get(None):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("XXcancel_urlXX"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("CANCEL_URL"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = None
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["XXcancel_urlXX"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["CANCEL_URL"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["XXcancel_urlXX"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["CANCEL_URL"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = None

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["XXapplication_contextXX"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["APPLICATION_CONTEXT"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = None
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post(None, json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post(json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("XX/v2/checkout/ordersXX", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/V2/CHECKOUT/ORDERS", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = None
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() and {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "XXsuccessXX": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "SUCCESS": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": False,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "XXorder_idXX": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "ORDER_ID": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get(None, ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", None),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get(""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("XXidXX", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("ID", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", "XXXX"),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_124(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "XXstatusXX": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_125(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "STATUS": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_126(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get(None, "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_127(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", None),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_128(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_129(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", ),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_130(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("XXstatusXX", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_131(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("STATUS", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_132(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "XXCREATEDXX"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_133(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "created"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_134(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "XXintentXX": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_135(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "INTENT": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_136(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "XXdataXX": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_137(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "DATA": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_138(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = None
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_139(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error and resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_140(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = None
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_141(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get(None, "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_142(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", None)
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_143(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_144(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", )
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_145(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("XXmessageXX", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_146(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("MESSAGE", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_147(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "XXXX")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_148(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail or isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_149(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_150(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = None
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_151(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get(None, [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_152(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", None)
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_153(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get([])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_154(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", )
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_155(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("XXdetailsXX", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_156(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("DETAILS", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_157(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = None
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_158(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get(None, str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_159(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", None) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_160(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get(str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_161(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", ) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_162(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[1].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_163(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("XXdescriptionXX", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_164(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("DESCRIPTION", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_165(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(None)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_166(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(None)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_167(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"XXsuccessXX": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_168(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"SUCCESS": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_169(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": True, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_170(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "XXerrorXX": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_171(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "ERROR": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_172(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_173(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_174(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": True, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_175(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_create_order__mutmut_176(self, params: dict[str, Any]) -> dict[str, Any]:
        """Crea una orden de pago en PayPal.

        Args:
            params: Debe contener 'intent' (CAPTURE/AUTHORIZE), 'amount', 'currency'
        """
        intent = params.get("intent", "CAPTURE")
        amount = params.get("amount", "")
        currency = params.get("currency", "USD")
        if not amount:
            return {"success": False, "error": "Parametro requerido: amount"}
        self._log_operation("create_order", f"amount={amount} {currency}")

        payload: dict[str, Any] = {
            "intent": intent,
            "purchase_units": [
                {
                    "amount": {
                        "currency_code": currency,
                        "value": str(amount),
                    }
                }
            ],
        }
        if params.get("description"):
            payload["purchase_units"][0]["description"] = params["description"]
        if params.get("custom_id"):
            payload["purchase_units"][0]["custom_id"] = params["custom_id"]
        if params.get("return_url") or params.get("cancel_url"):
            app_context: dict[str, str] = {}
            if params.get("return_url"):
                app_context["return_url"] = params["return_url"]
            if params.get("cancel_url"):
                app_context["cancel_url"] = params["cancel_url"]
            payload["application_context"] = app_context

        try:
            resp = self._http.post("/v2/checkout/orders", json=payload)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ""),
                    "status": data.get("status", "CREATED"),
                    "intent": intent,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", "")
                if not detail and isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", str(resp.body)) if details_list else str(resp.body)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "ERROR": f"HTTP client error: {e}"}

    @_mutmut_mutated(mutants_xǁPaypalConnectorǁ_capture_order__mutmut)
    def _capture_order(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = None
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get(None, "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", None)
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", )
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("XXorder_idXX", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("ORDER_ID", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "XXXX")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"XXsuccessXX": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"SUCCESS": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": True, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "XXerrorXX": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "ERROR": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "XXParametro requerido: order_idXX"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "PARAMETRO REQUERIDO: ORDER_ID"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation(None, f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", None)

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation(f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", )

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("XXcapture_orderXX", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("CAPTURE_ORDER", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = None
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                None,
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json=None,
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = None
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() and {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = None
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = "XXXX"
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get(None):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("XXpurchase_unitsXX"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("PURCHASE_UNITS"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = None
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["XXpurchase_unitsXX"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["PURCHASE_UNITS"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][1]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = None
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get(None, {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", None)
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get({})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", )
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("XXpaymentsXX", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("PAYMENTS", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = None
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get(None, [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", None)
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get([])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", )
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("XXcapturesXX", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("CAPTURES", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = None
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get(None, "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", None)
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", )
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[1].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("XXidXX", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("ID", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "XXXX")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "XXsuccessXX": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "SUCCESS": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": False,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "XXorder_idXX": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "ORDER_ID": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "XXstatusXX": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "STATUS": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get(None, "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", None),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", ),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("XXstatusXX", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("STATUS", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "XXCOMPLETEDXX"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "completed"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "XXcapture_idXX": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "CAPTURE_ID": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "XXdataXX": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "DATA": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = None
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error and resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = None
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get(None, str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", None)
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get(str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", )
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("XXmessageXX", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("MESSAGE", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(None))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = None
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get(None, [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", None)
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get([])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", )
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("XXdetailsXX", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("DETAILS", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = None
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get(None, detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", None) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get(detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", ) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[1].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("XXdescriptionXX", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("DESCRIPTION", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"XXsuccessXX": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"SUCCESS": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": True, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "XXerrorXX": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "ERROR": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": True, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_capture_order__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Captura una orden de pago autorizada.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("capture_order", f"order={order_id}")

        try:
            resp = self._http.post(
                f"/v2/checkout/orders/{order_id}/capture",
                json={},
            )
            if resp.ok:
                data = resp.json() or {}
                capture_id = ""
                if data.get("purchase_units"):
                    pu = data["purchase_units"][0]
                    payments = pu.get("payments", {})
                    captures = payments.get("captures", [])
                    if captures:
                        capture_id = captures[0].get("id", "")
                return {
                    "success": True,
                    "order_id": order_id,
                    "status": data.get("status", "COMPLETED"),
                    "capture_id": capture_id,
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "ERROR": f"HTTP client error: {e}"}

    @_mutmut_mutated(mutants_xǁPaypalConnectorǁ_get_order__mutmut)
    def _get_order(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = None
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get(None, "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", None)
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", )
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("XXorder_idXX", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("ORDER_ID", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "XXXX")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"XXsuccessXX": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"SUCCESS": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": True, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "XXerrorXX": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "ERROR": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "XXParametro requerido: order_idXX"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "PARAMETRO REQUERIDO: ORDER_ID"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation(None, f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", None)

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation(f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", )

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("XXget_orderXX", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("GET_ORDER", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = None
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = None
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() and {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "XXsuccessXX": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "SUCCESS": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": False,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "XXorder_idXX": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "ORDER_ID": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get(None, order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", None),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get(order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", ),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("XXidXX", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("ID", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "XXstatusXX": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "STATUS": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get(None, ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get(""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("XXstatusXX", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("STATUS", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", "XXXX"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "XXdataXX": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "DATA": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = None
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error and resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = None
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get(None, str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", None)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get(str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", )
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("XXmessageXX", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("MESSAGE", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(None))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"XXsuccessXX": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"SUCCESS": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": True, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "XXerrorXX": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "ERROR": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": True, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_order__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de una orden.

        Args:
            params: Debe contener 'order_id'
        """
        order_id = params.get("order_id", "")
        if not order_id:
            return {"success": False, "error": "Parametro requerido: order_id"}
        self._log_operation("get_order", f"order={order_id}")

        try:
            resp = self._http.get(f"/v2/checkout/orders/{order_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "order_id": data.get("id", order_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "ERROR": f"HTTP client error: {e}"}

    @_mutmut_mutated(mutants_xǁPaypalConnectorǁ_refund_payment__mutmut)
    def _refund_payment(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = None
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get(None, "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", None)
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", )
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("XXcapture_idXX", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("CAPTURE_ID", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "XXXX")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"XXsuccessXX": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"SUCCESS": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": True, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "XXerrorXX": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "ERROR": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "XXParametro requerido: capture_idXX"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "PARAMETRO REQUERIDO: CAPTURE_ID"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation(None, f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", None)

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation(f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", )

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("XXrefund_paymentXX", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("REFUND_PAYMENT", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = None
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") or params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get(None) and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("XXamountXX") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("AMOUNT") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get(None):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("XXcurrencyXX"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("CURRENCY"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = None
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["XXamountXX"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["AMOUNT"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "XXvalueXX": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "VALUE": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(None),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["XXamountXX"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["AMOUNT"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "XXcurrency_codeXX": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "CURRENCY_CODE": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get(None, "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", None),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", ),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("XXcurrencyXX", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("CURRENCY", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "XXUSDXX"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "usd"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get(None):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("XXnoteXX"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("NOTE"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = None

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["XXnoteXX"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["NOTE"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["XXnoteXX"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["NOTE"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = None
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                None,
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=None,
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = None
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() and {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "XXsuccessXX": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "SUCCESS": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": False,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "XXrefund_idXX": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "REFUND_ID": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get(None, ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", None),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get(""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("XXidXX", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("ID", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", "XXXX"),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "XXstatusXX": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "STATUS": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get(None, "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", ),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("XXstatusXX", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("STATUS", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "XXCOMPLETEDXX"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "completed"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "XXdataXX": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "DATA": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = None
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error and resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = None
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get(None, str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", None)
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get(str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", )
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("XXmessageXX", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("MESSAGE", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(None))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = None
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get(None, [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", None)
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get([])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_103(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", )
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_104(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("XXdetailsXX", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_105(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("DETAILS", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_106(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = None
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_107(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get(None, detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_108(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", None) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_109(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get(detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_110(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", ) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_111(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[1].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_112(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("XXdescriptionXX", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_113(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("DESCRIPTION", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_114(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"XXsuccessXX": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_115(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"SUCCESS": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_116(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": True, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_117(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "XXerrorXX": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_118(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "ERROR": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_119(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_120(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_121(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": True, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_122(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_refund_payment__mutmut_123(self, params: dict[str, Any]) -> dict[str, Any]:
        """Reembolsa un pago capturado.

        Args:
            params: Debe contener 'capture_id' y opcionalmente 'amount', 'note'
        """
        capture_id = params.get("capture_id", "")
        if not capture_id:
            return {"success": False, "error": "Parametro requerido: capture_id"}
        self._log_operation("refund_payment", f"capture={capture_id}")

        payload: dict[str, Any] = {}
        if params.get("amount") and params.get("currency"):
            payload["amount"] = {
                "value": str(params["amount"]),
                "currency_code": params.get("currency", "USD"),
            }
        if params.get("note"):
            payload["note"] = params["note"]

        try:
            resp = self._http.post(
                f"/v2/payments/captures/{capture_id}/refund",
                json=payload if payload else {},
            )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "refund_id": data.get("id", ""),
                    "status": data.get("status", "COMPLETED"),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                if isinstance(error_body.get("details"), list):
                    details_list = error_body.get("details", [])
                    detail = details_list[0].get("description", detail) if details_list else detail
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "ERROR": f"HTTP client error: {e}"}

    @_mutmut_mutated(mutants_xǁPaypalConnectorǁ_list_transactions__mutmut)
    def _list_transactions(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation(None)

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("XXlist_transactionsXX")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("LIST_TRANSACTIONS")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = None
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = None
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get(None, "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", None)
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", )
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("XXstart_dateXX", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("START_DATE", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "XXXX")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = None
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get(None, "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", None)
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", )
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("XXend_dateXX", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("END_DATE", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "XXXX")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = None
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["XXstart_dateXX"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["START_DATE"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = None
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["XXend_dateXX"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["END_DATE"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get(None):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("XXlimitXX"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("LIMIT"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = None
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["XXpage_sizeXX"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["PAGE_SIZE"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["XXlimitXX"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["LIMIT"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get(None):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("XXpageXX"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("PAGE"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = None

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["XXpageXX"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["PAGE"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["XXpageXX"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["PAGE"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = None
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get(None, params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get(params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", )
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("XX/v1/reporting/transactionsXX", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/V1/REPORTING/TRANSACTIONS", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = None
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() and {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "XXsuccessXX": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "SUCCESS": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": False,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "XXtransactionsXX": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "TRANSACTIONS": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get(None, []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", None),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get([]),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", ),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("XXtransaction_detailsXX", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("TRANSACTION_DETAILS", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "XXtotalXX": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "TOTAL": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get(None, 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", None),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get(0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", ),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("XXtotal_itemsXX", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("TOTAL_ITEMS", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 1),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "XXpageXX": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "PAGE": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get(None, 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", None),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get(1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", ),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("XXpageXX", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("PAGE", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 2),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "XXdataXX": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "DATA": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = None
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error and resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = None
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get(None, str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", None)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get(str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", )
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("XXmessageXX", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("MESSAGE", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(None))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"XXsuccessXX": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"SUCCESS": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": True, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_96(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "XXerrorXX": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_97(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "ERROR": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_98(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_99(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_100(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": True, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_101(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_list_transactions__mutmut_102(self, params: dict[str, Any]) -> dict[str, Any]:
        """Lista transacciones de PayPal.

        Args:
            params: Opcionalmente 'start_date', 'end_date', 'limit', 'page'
        """
        self._log_operation("list_transactions")

        query_params: dict[str, Any] = {}
        start_date = params.get("start_date", "")
        end_date = params.get("end_date", "")
        if start_date:
            query_params["start_date"] = start_date
        if end_date:
            query_params["end_date"] = end_date
        if params.get("limit"):
            query_params["page_size"] = params["limit"]
        if params.get("page"):
            query_params["page"] = params["page"]

        try:
            resp = self._http.get("/v1/reporting/transactions", params=query_params if query_params else None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "transactions": data.get("transaction_details", []),
                    "total": data.get("total_items", 0),
                    "page": data.get("page", 1),
                    "data": data,
                }
            else:
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "ERROR": f"HTTP client error: {e}"}

    @_mutmut_mutated(mutants_xǁPaypalConnectorǁ_get_payment_details__mutmut)
    def _get_payment_details(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_orig(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_1(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = None
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_2(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get(None, "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_3(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", None)
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_4(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_5(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", )
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_6(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("XXpayment_idXX", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_7(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("PAYMENT_ID", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_8(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "XXXX")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_9(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_10(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"XXsuccessXX": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_11(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"SUCCESS": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_12(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": True, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_13(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "XXerrorXX": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_14(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "ERROR": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_15(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "XXParametro requerido: payment_idXX"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_16(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_17(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "PARAMETRO REQUERIDO: PAYMENT_ID"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_18(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation(None, f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_19(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", None)

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_20(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation(f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_21(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", )

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_22(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("XXget_payment_detailsXX", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_23(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("GET_PAYMENT_DETAILS", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_24(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = None
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_25(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(None)
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_26(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = None
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_27(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() and {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_28(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "XXsuccessXX": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_29(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "SUCCESS": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_30(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": False,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_31(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "XXpayment_idXX": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_32(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "PAYMENT_ID": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_33(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get(None, payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_34(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", None),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_35(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get(payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_36(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", ),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_37(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("XXidXX", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_38(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("ID", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_39(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "XXstatusXX": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_40(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "STATUS": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_41(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get(None, ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_42(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", None),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_43(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get(""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_44(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_45(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("XXstatusXX", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_46(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("STATUS", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_47(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", "XXXX"),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_48(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "XXdataXX": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_49(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "DATA": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_50(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = None
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_51(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(None)
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_52(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = None
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_53(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() and {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_54(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "XXsuccessXX": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_55(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "SUCCESS": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_56(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": False,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_57(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "XXpayment_idXX": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_58(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "PAYMENT_ID": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_59(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get(None, payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_60(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", None),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_61(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get(payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_62(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", ),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_63(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("XXidXX", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_64(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("ID", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_65(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "XXstatusXX": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_66(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "STATUS": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_67(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get(None, ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_68(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", None),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_69(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get(""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_70(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_71(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("XXstatusXX", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_72(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("STATUS", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_73(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", "XXXX"),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_74(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "XXdataXX": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_75(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "DATA": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_76(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = None
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_77(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error and resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_78(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = None
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_79(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get(None, str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_80(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", None)
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_81(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get(str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_82(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", )
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_83(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("XXmessageXX", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_84(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("MESSAGE", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_85(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(None))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_86(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"XXsuccessXX": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_87(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"SUCCESS": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_88(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": True, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_89(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "XXerrorXX": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_90(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "ERROR": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_91(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"XXsuccessXX": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_92(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"SUCCESS": False, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_93(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": True, "error": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_94(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "XXerrorXX": f"HTTP client error: {e}"}

    def xǁPaypalConnectorǁ_get_payment_details__mutmut_95(self, params: dict[str, Any]) -> dict[str, Any]:
        """Obtiene los detalles de un pago.

        Args:
            params: Debe contener 'payment_id'
        """
        payment_id = params.get("payment_id", "")
        if not payment_id:
            return {"success": False, "error": "Parametro requerido: payment_id"}
        self._log_operation("get_payment_details", f"payment={payment_id}")

        try:
            resp = self._http.get(f"/v2/payments/captures/{payment_id}")
            if resp.ok:
                data = resp.json() or {}
                return {
                    "success": True,
                    "payment_id": data.get("id", payment_id),
                    "status": data.get("status", ""),
                    "data": data,
                }
            else:
                # Try authorizations endpoint as fallback
                resp2 = self._http.get(f"/v2/payments/authorizations/{payment_id}")
                if resp2.ok:
                    data = resp2.json() or {}
                    return {
                        "success": True,
                        "payment_id": data.get("id", payment_id),
                        "status": data.get("status", ""),
                        "data": data,
                    }
                error_body = resp.json() if resp.is_client_error or resp.is_server_error else {}
                detail = error_body.get("message", str(resp.body))
                return {"success": False, "error": f"HTTP {resp.status_code}: {detail}"}
        except HTTPClientError as e:
            return {"success": False, "ERROR": f"HTTP client error: {e}"}

mutants_xǁPaypalConnectorǁ__init____mutmut['_mutmut_orig'] = PaypalConnector.xǁPaypalConnectorǁ__init____mutmut_orig # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ
# type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_11'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_11 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_12'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_12 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_13'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_13 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_14'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_14 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_15'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_15 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_16'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_16 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_17'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_17 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_18'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_18 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_19'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_19 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_20'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_20 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_21'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_21 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_22'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_22 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_23'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_23 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_24'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_24 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_25'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_25 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_26'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_26 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_27'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_27 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_28'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_28 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_29'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_29 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_30'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_30 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_31'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_31 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_32'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_32 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_33'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_33 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_34'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_34 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_35'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_35 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_36'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_36 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_37'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_37 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_38'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_38 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_39'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_39 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_40'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_40 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_41'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_41 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_42'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_42 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_43'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_43 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_44'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_44 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_45'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_45 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_46'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_46 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_47'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_47 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_48'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_48 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_49'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_49 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ
# type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_89'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_89 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_90'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_90 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_91'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_91 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_92'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_92 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_93'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_93 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_94'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_94 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_95'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_95 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_96'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_96 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_97'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_97 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_98'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_98 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_99'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_99 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_100'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_100 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_101'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_101 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_list_transactions__mutmut['xǁPaypalConnectorǁ_list_transactions__mutmut_102'] = PaypalConnector.xǁPaypalConnectorǁ_list_transactions__mutmut_102 # type: ignore # mutmut generated

mutants_xǁPaypalConnectorǁ_get_payment_details__mutmut['_mutmut_orig'] = PaypalConnector.xǁPaypalConnectorǁ_get_payment_details__mutmut_orig # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_get_payment_details__mutmut['xǁPaypalConnectorǁ_get_payment_details__mutmut_1'] = PaypalConnector.xǁPaypalConnectorǁ_get_payment_details__mutmut_1 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_get_payment_details__mutmut['xǁPaypalConnectorǁ_get_payment_details__mutmut_2'] = PaypalConnector.xǁPaypalConnectorǁ_get_payment_details__mutmut_2 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_get_payment_details__mutmut['xǁPaypalConnectorǁ_get_payment_details__mutmut_3'] = PaypalConnector.xǁPaypalConnectorǁ_get_payment_details__mutmut_3 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_get_payment_details__mutmut['xǁPaypalConnectorǁ_get_payment_details__mutmut_4'] = PaypalConnector.xǁPaypalConnectorǁ_get_payment_details__mutmut_4 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_get
# type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_get_payment_details__mutmut['xǁPaypalConnectorǁ_get_payment_details__mutmut_82'] = PaypalConnector.xǁPaypalConnectorǁ_get_payment_details__mutmut_82 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_get_payment_details__mutmut['xǁPaypalConnectorǁ_get_payment_details__mutmut_83'] = PaypalConnector.xǁPaypalConnectorǁ_get_payment_details__mutmut_83 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_get_payment_details__mutmut['xǁPaypalConnectorǁ_get_payment_details__mutmut_84'] = PaypalConnector.xǁPaypalConnectorǁ_get_payment_details__mutmut_84 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_get_payment_details__mutmut['xǁPaypalConnectorǁ_get_payment_details__mutmut_85'] = PaypalConnector.xǁPaypalConnectorǁ_get_payment_details__mutmut_85 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_get_payment_details__mutmut['xǁPaypalConnectorǁ_get_payment_details__mutmut_86'] = PaypalConnector.xǁPaypalConnectorǁ_get_payment_details__mutmut_86 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_get_payment_details__mutmut['xǁPaypalConnectorǁ_get_payment_details__mutmut_87'] = PaypalConnector.xǁPaypalConnectorǁ_get_payment_details__mutmut_87 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_get_payment_details__mutmut['xǁPaypalConnectorǁ_get_payment_details__mutmut_88'] = PaypalConnector.xǁPaypalConnectorǁ_get_payment_details__mutmut_88 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_get_payment_details__mutmut['xǁPaypalConnectorǁ_get_payment_details__mutmut_89'] = PaypalConnector.xǁPaypalConnectorǁ_get_payment_details__mutmut_89 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_get_payment_details__mutmut['xǁPaypalConnectorǁ_get_payment_details__mutmut_90'] = PaypalConnector.xǁPaypalConnectorǁ_get_payment_details__mutmut_90 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_get_payment_details__mutmut['xǁPaypalConnectorǁ_get_payment_details__mutmut_91'] = PaypalConnector.xǁPaypalConnectorǁ_get_payment_details__mutmut_91 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_get_payment_details__mutmut['xǁPaypalConnectorǁ_get_payment_details__mutmut_92'] = PaypalConnector.xǁPaypalConnectorǁ_get_payment_details__mutmut_92 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_get_payment_details__mutmut['xǁPaypalConnectorǁ_get_payment_details__mutmut_93'] = PaypalConnector.xǁPaypalConnectorǁ_get_payment_details__mutmut_93 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_get_payment_details__mutmut['xǁPaypalConnectorǁ_get_payment_details__mutmut_94'] = PaypalConnector.xǁPaypalConnectorǁ_get_payment_details__mutmut_94 # type: ignore # mutmut generated
mutants_xǁPaypalConnectorǁ_get_payment_details__mutmut['xǁPaypalConnectorǁ_get_payment_details__mutmut_95'] = PaypalConnector.xǁPaypalConnectorǁ_get_payment_details__mutmut_95 # type: ignore # mutmut generated


PAYPAL_SCHEMA = ConnectorSchema(
    name="paypal",
    version="1.0.0",
    description="Crea y gestiona pagos, ordenes y transacciones via PayPal",
    category="finance_payments",
    icon="credit-card",
    author="Zenic-Flijo",
    actions=[
        ActionDefinition(name="create_order", description="Crea una orden de pago", category="write"),
        ActionDefinition(name="capture_order", description="Captura una orden", category="write"),
        ActionDefinition(name="get_order", description="Obtiene detalles de orden", category="read"),
        ActionDefinition(name="refund_payment", description="Reembolsa un pago", category="write"),
        ActionDefinition(name="list_transactions", description="Lista transacciones", category="read"),
        ActionDefinition(name="get_payment_details", description="Obtiene detalles de pago", category="read"),
    ],
    auth_requirements=[
        AuthRequirement(auth_type="oauth2", required_fields=["client_id", "client_secret"], description="PayPal OAuth2 Credentials")
    ],
)
